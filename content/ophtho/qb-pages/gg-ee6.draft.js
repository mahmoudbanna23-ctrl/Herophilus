/* GG "End Exam 6 (Photos)" — 35 entries, opqb-t26-1 … -35. NOTHING FOLDED.
   Bare comma-separated run of object literals: no wrapper, no trailing comma.
   Staging record: gg-ee6.array.js. Book pp.151–159 (PDF 158–166), keys on p.159.
   35 printed = 35 promised = 35 keyed. 0 printed explanation boxes ⇒ a marker on
   every entry.

   THIS IS THE MODULE'S ONLY PICTURE SECTION. Every entry carries image / imgAlt /
   imgEssential:true. Basenames are `q-op-gg-ee6-<n>`, matching the printed question
   number — the `ee6` is load-bearing, because `q-op-gg-11.jpg` already exists and
   belongs to `opqb-t2-95`. All 35 crops were confirmed present in app\assets\q\
   before writing. `imgAlt` states modality and view ONLY; the diagnostic read is in
   `explanation`, deliberately, so the alt text cannot answer the stem.

   ID SPACE CHECKED BEFORE WRITING: grep -c "opqb-t26-" app/data/questions.ophtho.js
   returned 0. Cross-references are restricted to ids that already exist —
   `opmcq-c1-*`…`opmcq-c20-*` and `opqb-t1-*`…`opqb-t25-*` (End Exam 5 was spliced
   and committed 2026-09-02, so t25 IS live; the brief's §7 saying otherwise is
   stale). `opqb-t27-*` and `opqb-t28-*` are still being built and are NOT cited.

   PRINTED KEY, book p.159, in order 1–35, taken from the staging:
     A A D B A B B C B C  A C B A B C A C D B  C A A A A A A A A A  A A D C B
   Q22–Q32 are ELEVEN consecutive 'A'. Read three times, twice by the staging pass
   and once again by an independent page audit before writing. NOT corrected —
   a defective key is noted, never disputed, and `answer` never moves.

   OPTION COUNTS: 34 questions print four options (A–D); Q32 prints THREE (A–C).
   `answer` is a zero-based index into `options` as written.

   Q1 and Q21 print the SAME photograph and ask different things of it. They are
   NOT duplicates and are NOT folded; both carry their own crop.

   PROGRESS MARKER: this file is appended in blocks; the last id present is how far
   it got. */
