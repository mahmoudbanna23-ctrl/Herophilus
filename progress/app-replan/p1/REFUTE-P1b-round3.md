REFUTE P1b round 3 - FAIL - 2026-09-26
Doc: progress/app-replan/p1/P1b-sync-design.md (232 lines, untracked; round-2 text no longer exists anywhere)

CITATIONS: all app/js cites checked, text matches (gate.js 133,135-140,186-198,202-244,221-223,268-296,292-294; storage.js 44,60-69,73,77-86,136-137; quiz.js 527-536; sessions.js 334-350; boot.js 260-262,266-291; clepsydra.js 618-625). Mislabels: L155 calls gate.js:197 a "roster-doc delete" - it deletes the profiles/{id} doc. L139 calls setSync('err') a "persistent sync-error toast" - it is a rail label "Sync failed - retry on next change" (gate.js:72) + 180 s retry (gate.js:59); no toast, no reload.

DEFECTS
N1 L56-57: kind list names answers,notes,reports,resumeByChapter,mockResume,term + schedule + banks/sessLog/sess/editSched. srs, hl, flags, conf, days have NO kind entry or merge rule (grep: only L19 mentions conf/srs). "as round 1 (unchanged)" points at text that was overwritten (file untracked, no git history). Brief item 1 core kinds missing; hl (array per qid) merge/tombstone shape absent. Fix: list each with id + value + rule inline.
N2 L129-139 vs ruling 2 ("old client is made to reload before it can sync"): doc says "no forced reload, just increasing irrelevance". Today's code has no path to a reload: failed write -> setSync('err') -> rail label + retrySync every 180 s (gate.js:59,72,156-161,320). Meanwhile, with no rules change, old clients keep syncing legacy state among themselves and show "Synced". Fix: step 3 ships firestore.rules denying writes of legacy `state` on users/{uid}/profiles/{pid} and root `profiles`; state honestly that old code can only show "Sync failed", never reload; or raise to owner.
N3 no firestore.rules change anywhere. progress/firestore.rules allows only users/{uid} and users/{uid}/profiles/{pid}, default-deny everything else. users/{uid}/profiles/{pid}/sync/v1 and users/{uid}/rosterV1/{pid} are DENIED -> every v1 read/write fails. Fix: step 3 adds match blocks + rules deploy + check.
N4 L102-110,118-121: fill-only is judged against LOCAL records only; seeded clock = updatedAt, which old save() stamps Date.now() (storage.js:137). Replay: Y(v1) unflags q1 day 10; X old client offline, answers other q day 12 (S.updatedAt=day12); X reloads day 13 -> migration seeds flags.q1 clock [day12,0,X] -> LWW beats Y's day-10 record -> q1 re-flagged everywhere. Same for tombstoned hl/marks. Same for legacy-format import on a device behind cloud. Round-2 D1 reopens via migration/import. Fix: legacy-origin records rank below any v1 envelope (flag or clock floor) in the NETWORK merge, not only locally; drop on remote conflict.
N5 L151-156: delete writes tombstone to rosterV1 + sync/v1, then explicitly deletes those docs. The tombstone is destroyed immediately (contradicts L90 1-year retention). B offline edits, reconnects: sync/v1 absent -> B's push recreates it -> profile resurrects. Scenario row L213 false. Fix: keep rosterV1 {del:true,c} doc 1 y; delete record payload only; v1 push checks rosterV1 del first.
N6 L142-149: "first account to link (by c clock) owns" - A cannot read users/B/rosterV1 (rules: uid==auth.uid). Device1 signs in A first, Device2 signs in B first: both auto-link same pid, each ownerUid=self. Then Device1 switches to B: local P.ownerUid=A vs B's rosterV1/P ownerUid=B - no rule. Not deterministic. Also "either account observes it" impossible. Fix: global create-once doc (e.g. owners/{pid}, rules create-only) or define ownership per device and drop the cross-account claim.
N7 L47: rosterV1 {id,ownerUid,c,del} drops name + av that legacy rosterProfile carries (gate.js:176-178); L51-52 says new clients never read legacy profiles -> fresh device shows unnamed profiles. Fix: add name/av LWW.
N8 L120 S.syncV=1, L123 gate `if(!S.syncV)` vs L35 syncV in records key and L40-43 "neither is a field of S". Old tab open across deploy, loaded pre-migration, saves whole S -> S.syncV gone, sched index-keyed -> next new load re-runs migrateSched AND migration (new item ids -> duplicate schedule items), or, if records key is canonical, the old tab's edits in KEY are silently discarded (load-time KEY vs records reconciliation unspecified). Fix: gate on records-key syncV only; state load rule for KEY edits (fill-only + warning).
N9 L123-127,L176-189: step 1 ships stable ids + migration while cloud is still legacy whole-S pushCloud (v1 path is step 3). Old clients on other devices pull S.sched `date|<dev>-<w>-<l>`: old migrateSched (storage.js:80,83 - key does not match /\|\d+$/) writes `|0`+`|1`; old delItem (sessions.js:342-346) +"dev-w-l" = NaN -> key `date|NaN`. Gate exists only in new code. D9 NOT closed. Step 2 per-record merge: transport undefined (legacy state still live) -> records vs pulled S diverge. Fix: until step 3, keep ids + envelopes in the records key only and materialise S.sched as date|index; or merge steps 1-3 transport.
N10 L31-32 records key "never inside ... cloud, export, import" vs L45 cloud = {syncV,records} and L113 v1 export merges "by its own envelope clocks". Contradiction. Fix: say the DEVICE key never travels; records do.
N11 L78-82: clamp limits local HLC but L82 keeps sender's far-future clock on the record -> that record beats every honest edit for years; tombstone kept until far-future+1 y. "still >24h ahead after clamping" (L81) can never be true. Fix: compare with clamped wall, or put() issues w > existing record wall.
N12 L95-99: never-migrated device (old client offline >1 y, then reload) has no lastSyncedAt -> stale gate undefined -> pushes >1y records whose remote tombstones were GC'd. GC runner up to 24 h fast GCs before a 1 y stale threshold trips. Unsynced >1 y local edits dropped, not disclosed. Fix: missing lastSyncedAt = stale; stale threshold = retention - 48 h.
N13 import legacy export >1 y old: local tombstones GC'd -> fill-only resurrects deleted items, pushes them. Fix: skip fill with seed clock < now-1y.
Minor: schedule item->day binding, order within day, day delete vs offline item add unspecified (L57-61). Records key has no bak snapshot / corrupt-key rescue. window.storage ('cloud' storageMode, storage.js:51-54) not addressed for new keys.

CONSTRAINTS: Firebase optional kept; 8 s/15 s kept (L173); file:// ok; no wardround.* rename (new keys wardround.sync.*); theme/clep untouched; loadState rescue unchanged (L168-169). sess sync = owner ruling 1, accepted.

SCENARIOS
A offline edit vs B delete | record LWW + tombstone | converges; schedule day/item orphan rule unspecified | OK (minor)
Equal clocks | deviceId tie | converges | OK
1 h fast | HLC, clamp not reached | converges | OK (>24 h: N11 FAIL)
Old client after new | no reload gate; migration seed | old keeps "Synced" on legacy; on reload seeded updatedAt clocks beat v1 (N2,N4) | FAIL
Offline >1 y | per-profile lastSyncedAt | never-migrated device ungated; skew GC (N12) | FAIL
Import old export | fill-only local-only | beats remote v1; >1 y resurrects (N4,N13); v1 export contradiction N10 | FAIL
Profile del A / B offline | tombstone then doc delete | tombstone destroyed, B recreates (N5) | FAIL
Account switch shared legacy | first-link by c | unenforceable cross-uid; two owners (N6) | FAIL
Old tab whole-S save | records key isolated | S.syncV erased -> re-migration/dup ids or silent loss (N8) | FAIL
Force-upgrade gate | none in code | rail "Sync failed", never reload; v1 paths denied by rules (N2,N3) | FAIL
Step 1 alone | gate in new code only | old clients mangle ids, date|NaN (N9) | FAIL
