1. **Mascot: fully absent on quiz/review/reading.** A/B are strongest: threshold speech is still an interruption. C/D mistake “small” for “quiet”; continuity belongs at session boundaries, not during answers.

2. **Water: hybrid rule.** B is strongest: separate indicator while the fixed 2D asset remains; five baked vessel states only if 3D is approved. A wrongly defers useful current feedback; C/D omit the failed-3D-pilot path. Define reset time, denominator, and repeat-answer handling.

3. **Drop IndexedDB, but make P9 prove complete offline availability.** A is strongest because it catches the real hole: “visited subject works” is weaker than “entire intended corpus is available.” C/D assert “no benefit” without addressing interrupted cache population, version changes, or stale assets.

4. **Baked-background H.264 MP4.** A/B are strongest. Transparency adds format and compositing failure modes without a stated visual need. Require tested loop boundaries, dimensions/cropping, mute/autoplay, phone/tablet playback, and battery impact.

5. **Mock-ups first; baseline/gate before implementation.** A has the better distinction: static mocks fulfill the owner’s request without entangling them with broken runtime behavior. B/C/D incorrectly make visual choice wait on technical repairs. P0/P1 remain mandatory before any functional phase.

6. **Keep muted borders as the plan, but include one explicitly labelled neon comparison in P3.** B best balances a superseded decision with a recent owner signal. Do not silently restore neon or create an open-ended style debate.

7. **Keep the freeze, rename and specify it.** B/A correctly identify that listed dates describe an exam-window deploy freeze, not merely eves. State inclusive protected dates per exam, whether urgent fixes are permitted, and whether local changes may be made but not merged/deployed.

**REQUIRES OWNER APPROVAL**

- 3D identity fidelity, source/licence/spending, and whether a failed 3D pilot means retaining PNG mascot assets indefinitely.
- Any neon restoration beyond the single P3 comparison.

**Missed by all proposals**

- Service workers do not operate under `file://`; acceptance tests must keep disk mode and hosted-installed mode as separate products, not treat one as a fallback for the other.
- Define cache quota/failure behavior: if full offline corpus cannot cache, show an honest state rather than claiming offline readiness.
- Make the release gate version-aware: HTML, question scripts, manifest, and service-worker cache must identify one compatible release; otherwise mixed old/new clients can load silently.
- Split P9 into offline shell/data lifecycle versus auxiliary pages, and put accessibility acceptance criteria into each UI phase rather than leaving them all to P10.