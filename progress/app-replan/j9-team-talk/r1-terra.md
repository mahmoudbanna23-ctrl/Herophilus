1. Fork positions

1. **Quiz mascot: fully absent.** Quiz/review/reading are expressly “plain and quiet”; even threshold speech adds state and visual distraction where attention is scarce. Keep the threshold logic available for home/subject/session boundaries only.

2. **Water level: separate beside PNG vessel; five baked states for 3D.** This respects the fixed art and avoids brittle masking. Define today’s denominator and reset boundary in code/tests, otherwise “100%” is not verifiable.

3. **Drop IndexedDB: sound, with one caveat.** Service-worker cache covers hosted offline use and disk already has local data. The P9 test must prove an installed app can open a previously used subject offline after a reload, not merely that installation succeeds.

4. **Baked-background H.264 loops.** It is the least risky path for Safari and `file://`; require exact shared scene plate, dimensions, and first/last-frame alignment so clip swaps do not visibly jump.

5. **P0/P1 before P3.** Mock-ups need a reliable way to open offline and a known baseline; otherwise their review is contaminated by existing failures. P3 can be visually designed while P0/P1 run, but should not be presented as a choice until boot checks pass.

6. **Keep neon dropped, but show one owner-confirmation sample in P3.** The record says it is superseded; reopening it as a live option invites churn. A narrowly labelled comparison lets the owner overrule deliberately.

7. **R9 is right in intent but misnamed/inconsistent.** The listed dates include exam days, not only eves; call it an “exam-window deploy freeze.” Local work should also exclude risky merges during that window unless there is a documented urgent fix.

Other issues: P8 combines two independently shippable features and should split into P8a lab values and P8b trends. P10 promises “CSP report shows no violations,” but Report-Only requires a reporting endpoint and observed browsing traffic; make the check “no violations in defined fixture journeys” unless such collection is implemented. P2 needs a script-load failure/duplicate-load test and a manifest version/cache-update strategy for P9.

2. Phase ownership

| Phase | Owner type | Why |
|---|---|---|
| P0 | Coding agent | Root-causing checks and corpus counts requires repo/tool access. |
| P1 | Coding agent | New validator, fixture, smoke page, and command integration. |
| P2 | Coding agent | Data-loader changes need count/performance regression checks. |
| P3 | Human owner | Screen choices are taste/usage decisions; coding agent prepares static mocks. |
| P4 | Coding agent | Existing app controls and measurable contrast/interaction work. |
| P5 | Coding agent | Responsive scene implementation and visual/performance checks. |
| P6 | Coding agent | Stateful trigger caps, storage keys, and reduced-motion behavior. |
| P7 | Human owner | Art licence and recognisability gate; a cheap single-shot model can draft generation briefs only. |
| P8 | Coding agent | Both features integrate existing local state; owner supplies/approves source boundary for lab values. |
| P9 | Coding agent | Service worker and Pages behavior need implementation and hosted verification. |
| P10 | Careful reviewer model | Security/a11y hardening benefits from adversarial review; coding agent applies approved fixes. |
| P11 | Coding agent | Theme implementation requires asset/CSS integration and contrast testing. |
| P12 | Careful reviewer model | Independent final refutation; human owner supplies the final experiential acceptance. |

3. Limits of this seat

I cannot inspect the repository, measurements, existing checks, artwork, owner decisions, Cloudflare configuration, or third-party licence terms. I therefore cannot verify claimed paths/line numbers, performance figures, browser behavior, or whether the phase checks actually pass. Assumption: the stated disk facts and ownership constraints are accurate.