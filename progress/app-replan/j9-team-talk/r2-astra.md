The supplied text ends midway through D; the full draft is absent. References to its sections, measurements, and rules remain claims for the manager to verify.

1. **Quiz mascot: A/B strongest—absent by default.** C/D assume a small figure is non-intrusive; size does not establish that. A/B also overextend by deciding review and reading screens without their requirements. My pick: absence during active questions, companionship at session boundaries. Verify route conditions and threshold triggers on disk.

2. **Water level: A’s conditional approach strongest.** B/C/D correctly avoid modifying the drawing, but “trivial” implementation cannot resolve A’s alleged vessel-only rule. Verify that rule first. If binding, defer until approved vessel states exist; otherwise a separate indicator is reasonable. Define target, repeat-answer counting, and day boundary before either implementation.

3. **Question cache: A strongest—drop IndexedDB, specify offline completeness.** B’s previously visited subject test is too weak for a whole-bank promise. C invents a history of cache bugs; D’s “third cache” count establishes no functional redundancy. Verify every question script remains loadable directly from disk and identify exactly which hosted files the service worker guarantees offline. Keep study-state storage separate from this decision.

4. **Video: baked-background MP4 strongest, but provisionally.** C’s “works everywhere” and “no perceptible benefit” are unsupported; D’s reported autoplay measurement cannot establish cross-device behavior. Verify actual assets, playback settings, responsive cropping, poster fallback, and scene matching. Transparent delivery becomes worthwhile only if approved layouts require independent positioning.

5. **Order: A strongest—isolated mock-ups first; baseline and gate before app changes.** B/C/D mistake visual proposals for production integration. Existing quiz bugs do not prevent reviewing static fixture screens. However, urgent study-blocking fixes should proceed independently, without waiting for design approval. Verify mock-ups cannot write real progress.

6. **Neon: A strongest—show one restrained comparison.** B/D’s owner-overrulable drop effectively admits the preference is unresolved; C treats a manager’s draft as owner consent. Verify the decision record and let the owner choose the same screen with muted versus static neon borders.

7. **Freeze: A/B strongest—protect the study experience, not merely deployment.** Explicitly cover September 26–27, October 2–3, and October 16–19 if freezing eves plus exam days. C/D do not resolve scope. Verify release automation and service-worker activation behavior; earlier releases can reach clients during a freeze.

8. **Scope: A strongest.** Split the 3D pilot from integration, reference content from trends, and offline infrastructure from miscellaneous pages. Accessibility must be checked again after theme changes; C’s proposed ordering alone cannot catch later regressions.

**What all proposals missed:** Define export/import and recovery for irreplaceable study progress before offline changes; inspect storage keys and migration code. Distinguish downloaded question availability from offline sign-in availability. Set a static, owner-approved mascot as a complete fallback so 3D never blocks shipping. Record release evidence separately for hosted and disk modes: passing one does not establish the other.