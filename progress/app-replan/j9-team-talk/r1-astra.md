These are proposals from the supplied brief; disk verification remains with the manager.

1. **Mascot: absent on quiz, review, and reading screens.** Threshold speech still interrupts concentration. Keep companionship on home and subject pages; verify placement and trigger suppression in those routes.

2. **Water: five states only after approved 3D; defer the PNG indicator.** A separate gauge contradicts “the only water level is in her vessel.” Define “today’s progress” first: denominator, day boundary, repeated answers, and behavior without a daily target.

3. **Drop the question-data IndexedDB cache.** The proposed division is coherent, but caching visited subjects does not prove the whole bank works offline. P9 needs an explicit offline-download scope, completion indicator, version consistency, and interrupted-download tests.

4. **Choose baked-background MP4.** It matches the fixed desk composition and avoids the proposed dual-format transparency pipeline. Require actual phone/tablet playback, crop, transition, and battery checks; laptop autoplay evidence is insufficient.

5. **Mock-ups first, then P0/P1 before implementation.** Static fixture mock-ups can satisfy the owner’s request without changing the app. P2 currently precedes screen approval unnecessarily; move it after the picks.

6. **Offer neon back as one restrained mock-up variant.** The brief records conflicting preferences. Present muted borders and a static neon edge on the same screen; the owner chooses.

7. **Keep the deployment freeze, correct its dates and scope.** For October 17–19 exams, explicitly freeze October 16–19 inclusive; the present notation is ambiguous and misses the eve. Protect the study copy from local replacement too, while allowing isolated work.

8. **Split oversized phases and move safeguards earlier.** P7 combines identity pilot, rigging, rendering, five states, and integration; P8 combines two independent features; P9 combines offline infrastructure and miscellaneous pages. Accessibility belongs in each screen’s acceptance checks.

Other problems the manager can verify from disk:

- P0 permits explained failures, while R8 requires green checks. Record which failures block release; “explained” must not mean waived.
- P1 references manifest counts before P2 creates the manifest. Move its definition into P1.
- P6’s retained idle facts and pose shuffle conflict with threshold-only silence. Specify route-level behavior.
- A PNG-to-video change is not proven to require only `poseSrc()`: inspect element creation, playback lifecycle, pause, and fallback handling.
- P9 needs upgrade, rollback, and offline-auth tests. A Git tag alone does not prove cached clients can recover.
- P10’s “0 AA failures” needs a defined manual checklist; a clean automated report is insufficient.
- P8 must explicitly retain weak chapters and the due list; “trends dashboard” does not establish coverage.

Worker ownership:

| Phase | Lead and reason |
|---|---|
| P0 | Repo coding agent: reproduce and fix failures. |
| P1 | Repo coding agent: executable gate and fixtures. |
| P2 | Repo coding agent: loading dependencies and counts. |
| P3 | Human owner: screen choice; agent prepares mocks. |
| P4 | Repo coding agent: controls and timing behavior. |
| P5 | Repo coding agent: layout and measured motion. |
| P6 | Repo coding agent: stateful triggers and persistence. |
| P7 | Human owner: identity, licence, spending decisions. |
| P8 | Repo coding agent; owner verifies reference sources. |
| P9 | Repo coding agent: offline lifecycle. |
| P10 | Careful reviewer: audit; agent implements findings. |
| P11 | Repo coding agent: theme integration. |
| P12 | Careful reviewer: independent evidence; owner accepts. |

Cheap single-shot calls suit bounded copy variants, not phase ownership.

My limits: I have not inspected files, reproduced measurements, seen the mascot, or verified licences, sources, browsers, or owner preferences.