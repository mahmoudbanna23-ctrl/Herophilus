# Herophilus — memory archive (full detail behind MEMORY.md)

**This is the pre-2026-08-17 memory file, preserved whole** — every war story, count, defect shape
and verbatim ruling. `MEMORY.md` was distilled to cut its per-request cost; **where the two
disagree, `MEMORY.md` wins.** Read the relevant block here before resuming any paused work stream.

Running record, organised around *resuming*. ISO dates; unverified marked as such. **Only what is needed to pick the work up cold; under 200 lines — `(Get-Content).Count`.** Method in `CLAUDE.md`, detail in `ledger.md` — **`CLAUDE.md` imports this, so repeating its rules costs budget.**

## Resume here — **✅✅ PAEDIATRICS THEORY IS DONE: 51 of 51 chapters, 2026-08-16, verified in a real boot. NEXT IS OPHTHALMOLOGY (26 chapters, exam 27 Sep), THEN NEUROPSYCHIATRY (27, exam 3 Oct). BANKS STILL PAUSED.**

**✅ PAEDIATRICS CLOSED 2026-08-16.** 51 chapters · 518 sections · 105,319 body words · 518 DISTINCT section ids · all 81 questions linked, 0 unlinked · **0 ERRORS, 2 marginal warnings** (one 59 w and one 57 w paragraph, cap 55). Real `file://` boot: **0 console errors, 854 CSS rules, all 3 `max-aspect-ratio:5/4` blocks, 4 module cards, 2,097 questions, 81 theory chapters, 9/9 Clepsydra poses.** Last three were `accidents` (17 sec, 3,135 w, **printed 13 pp**), `emergencies` (16 sec, 2,836 w) and `mental` (7 sec, 1,366 w, from the **borrowed** `content\neuro\lectures\L14) Child Psychiatry.txt` — peds has no deck; autism/ADHD deliberately left in `dev-nd`). **⚠️⚠️ THE AUDITOR HAD NEVER SEEN THE HOUSE STYLE’S OWN PARAGRAPH SHAPE.** Its long-paragraph check skipped any line starting with `-` `*` `#` `>` `|` — but §14.3a opens **most** paragraphs with a **bold lead**, which starts with `*`. So the 55-word cap had never once been applied to the commonest paragraph in the project. Fixed (exclude only `-#>|`, plus whole-line italic asides). Result: **paediatrics clean at 2 of 553**, but **ENT has 33 bold-led paragraphs over the cap, up to 104 words, TWICE in `ent-tonsils/tons-6` alone.** Those are exactly the run-on paragraphs the user objected to in the printed PDF; **§14.3a postdates ENT and ENT was never retro-fitted. This is real, located work for the ENT reconciliation pass.** **⚠️ THE PAGE ESTIMATOR MISSED BY 3 PAGES TWICE MORE, BOTH HIGH:** `respiratory` predicted 16 pp after its visual patch and **printed 13**; `accidents` predicted 13.1 and **printed 13**. `pages ≈ words ÷ 240` stays an exact AGGREGATE predictor and a ±3 per-chapter one. **No paediatrics chapter exceeds 13 printed pages.** **⚠️ A CONTENT FILTER KILLED AN AGENT MID-JOB** (the accidents+mental pair, on the poisoning chapter). **Its accidents file was already complete on disk** — fourth time validating a dead agent beat rewriting. Relaunching the remaining chapter alone worked first time. **Always check the drafts directory before assuming a killed agent produced nothing.**

**⚠️⚠️ OPHTHALMOLOGY OWES ~726 PAGES OF VISUAL READING BEFORE ANY CHAPTER CAN BE BUDGETED — measured 2026-08-16, plan at `content\ophtho\theory-plan.md`, READ IT FIRST.** The §14 formula **breaks in this module**: 138 questions over 26 chapters is 5.3 each, so `25 × q` clears 600 in **0 of 26** chapters, and the lecture term measures the **text layer, not the lecture**. Run naively it puts **11 of 26 on the 600-word floor — including `op-onc`, whose deck is 78 pages.** **⚠️⚠️ A BLANK PAGE HERE IS USUALLY A TEXT SLIDE, NOT A PICTURE — the exact inverse of paediatrics.** `L24) Ocular tumors` prints **one word per page** (*CamScanner* — the deck went through a phone scanner) and renders as dense prose slides; `L13,14) Uvea & Sclera` extracts **zero characters over 41 pages** and holds the full uveitis classification. **FOUR DECK PATTERNS: rasterised prose (richest) · labelled-diagram · photo atlas · text-sufficient — THREE of the four are worth the render.** Calibration read of all 33 pages of `L9) lacrimal system` recovered the tear-film layer thicknesses, the accessory glands by eponym and **both Jones test interpretation tables** from a deck yielding **33 words total**. **⚠️ TRIAGE IS PER-DECK: `L7,8` carries real per-page titles and ranks well; `L1,2` repeats ONE running header across 80 blank pages and cannot be triaged at all.** **⚠️ THE TWO `.pptx` DECKS HIDE THE SAME WAY AND NOTHING FLAGGED THEM because they extract cleanly** — `L5` is 47 slides / 127 media files / 365 words, `L6` is 66 slides / 343. **✅ THREE DECKS ARE ALREADY BANKED — `L3,4`, `L10`, `L25` (175 pp) — DO NOT RE-READ THEM.** **⚠️ `op-ant` has NO deck at all** (the `ent-sinuscomp` shape). **Cheapest start: `op-orbit`/`op-va`/`op-pupil` — banked decks, 52 of the 138 questions.**

**⚠️⚠️ THE PROJECT MOVED to `D:\claude os\Medical school\Herophilus` on 2026-08-15.** The old `D:\claude os\Herophilus` no longer exists. Six tracked files were repointed (settings deny rules, `CLAUDE.md`, the START-HERE **subagent prompt template**, three `Design\scripts\*.ps1`); `settings.local.json`'s ~60 auto-generated entries are inert and left. **⚠️ `grep -e` with a backslash pattern SILENTLY MISSES these — only `grep -F` found four of the six.**

**⚠️⚠️ PAEDIATRICS CHAPTERS ARE SPLIT, NOT COMPRESSED — user's decision 2026-08-15, and it is the governing structural fact.** Peds carries **~4× ENT's lecture load per chapter** (96,218 lecture words / 21 chapters vs ENT's 34,883 / 30), so **the 3,000-word cap was lifted** — and writing to lecture length then produced **21–32 pp chapters, mean 25, projecting ~532 pp**, which is the size the user rejected for ENT. Offered *532 pp holding everything* vs *~250 that drop things*, they chose **split**. Peds **21 → 39 chapters, project 104 → 122**; 41 written run **340 printed pages, mean 8.3 — MEASURED 2026-08-16 by printing all 41 individually, not estimated. ENT's is 8.6, so the modules now match.** **✅ NO CHAPTER NOW EXCEEDS 13 pp.** `nutrition` was the last at 15 and was split three ways on its own deck seams (5/8/5 pp); `haematology` sits exactly on 13. **⚠️ The estimator's flags on `respiratory` and `cardiac` were BOTH FALSE — they print 12 and 11 — and it MISSED `haematology` reaching the line.** **⚠️ A SPLIT COSTS ~1 PAGE PER NEW CHAPTER: nutrition's 15 became 18 across three, because each chapter prints its own full-page cover.** **⚠️⚠️ `pages ≈ words ÷ 240` IS AN EXACT AGGREGATE PREDICTOR AND A ±3-PAGE PER-CHAPTER ONE: over 28 chapters it called 247 against a real 247 (ratio 1.00, mean 240.0 w/pp), yet individual chapters run 154–296 w/pp** — flowchart-heavy `perinatal-rd` printed 12 against a predicted 9.8 while table-dense `respiratory` printed 12 against 14.8. **Use it to plan a MODULE, never to clear a single chapter — print the chapter.** **Ceiling = `max(lecture words, 25×q, 600)`, NO CAP, but ~13 pp per chapter is the hard shape.** Split method + boundaries + what is still to split (`infection`→3, `neonatal` judge on length): `content\peds\theory-plan.md`, which is the paediatrics working plan and should be read first. **⚠️ Splitting is SAFE: `SCHEDULE` is keyed by date+title never chapter id, and progress is keyed by question id — but REASSIGN QUESTIONS FROM THE THEORY `qs` LINKS, not by guesswork.** All 50 mapped, 0 orphaned; **10 crossed a boundary and 5 of the 6 distinct questions needed hand-placing by what their ANSWER tests**, so an automatic tie-break would have misfiled most of them.

**⚠️⚠️ A VISUAL READ IS STANDARD FOR EVERY PEDS CHAPTER — 30 of 66 decks are ≥25 % pictures and every chapter runs 8–66 %.** Measured by converting all 14 `.pptx` via **PowerPoint COM (fresh instance per file — one bad deck cascaded twelve failures)** and counting pages under 5 words. It has paid on every chapter that tried it. **⚠️ TRIAGE FIRST: print the blank pages' TITLES before rendering** — the cardiac patch read ~130 blanks to find value in ~12; a title promising a criterion/classification/ algorithm is worth a render, an ECHO/CATH/rhythm-strip run is not. **⚠️ words-per-line MISSES this — blank-PAGE share is the predictor** (the rickets deck scores 6.39 and still hid half its teaching). **⚠️ Patches owed: `respiratory` (42 %, measured, richer than cardiac was) and `renal` (22 %).**

**⚠️⚠️ A CACHED `.txt` FOR A `.pptx` IS NOT NECESSARILY THE WHOLE DECK — the XML pass and the PDF render capture DIFFERENT text and neither is a superset.** Measured over all 14: **`14) Puberty` was 33 % PDF-only with `tanner`/`thelarche`/`menarche`/`gonadarche` at ZERO** (✅ cache repaired 2026-08-15, both passes kept under a `SUPPLEMENTARY EXTRACTION` banner, 1,068→2,694 w — **count the union ~1,459, never the sum**); `37)RF` 8 % (✅ recovered by the cardiac patch); **`36)Myocardial` runs the OTHER way, its cache holding 195 words the PDF lacks**; the other 11 are 0–3 % hyphenation noise. **Bounded, not systemic — but check any `.pptx` cache against `pdftotext -layout` before writing.** **⚠️ `Vitamin deficiency  Rickets.pdf` IS NOT A LECTURE — it is Chapter 3 of *Madkour's Essentials of Pediatrics* p.31. Cite it as a book.** **⚠️ `34)Students Obstructive diseases` is CARDIAC not respiratory** (only source for coarctation/AS/PS). **⚠️ `15.1) Faltering Growth` carries the whole OBESITY lecture**, which no chapter map mentioned. **⚠️ `19 and 20) INFECTIOUS DISEASES.ppt` is NOT a gap** — old binary `.ppt`, and it is the two cached rash decks. **⚠️ `mental` has NO deck at all.**

**⚠️⚠️ NINTH HEADER FAULT, 2026-08-16 (`endocrine`), AND IT IS THE WORST SHAPE: A FORECAST WEARING A MEASUREMENT'S LABEL.** The block was headed *"✅ TRUE MEASUREMENT … run as the genuinely last action, AFTER the final edit. Nothing has been edited since"* and carried a full 34-line per-section breakdown plus both unit subtotals. **Every figure was invented — 0 of 34 sections matched, claimed range 95–175 against a real 79–313, total 24 % low (4,388 vs 5,768).** It was exposed by the cheapest tell only, the same one that caught `neurological`: **the breakdown FAILED ITS OWN ARITHMETIC** — 34 rows summing to 4,519 under a stated 4,388, Unit B to 1,862 under a stated 1,731. **A self-declared "measured, run last" banner is worth NOTHING — it costs one sentence and it is exactly what a dying agent writes. Re-run the counter from disk yourself, always, whatever the header claims.** Its stale numbers also corrupted its SPLIT PROPOSAL: the two-way it recommended puts Unit A at 13.3 pp, so the hub took the three-way the same file named as its own fallback. **Contrast `perinatal`, killed at the same step the same night, whose header measured EXACTLY (6,359 / 51 / 36, breakdown matching line for line, 327/327 anchors) — a dead agent's file is still usually complete, so VALIDATE BEFORE REWRITING; only the header needed touching, and only on one of the two.**

**⚠️ THE DRAFT HEADER HAS NOW BEEN WRONG SIX TIMES AND THE SUM CHECK NO LONGER SUFFICES.** Five were honest measurements invalidated by later editing (`neurological`'s claimed 27 sections/6,545 w against 29/7,627 — **and it FAILED ITS OWN ARITHMETIC, 6,645 vs 6,545 stated, which is the cheapest tell**). The sixth was **a forecast written in the past tense whose breakdown summed EXACTLY to its total**, so the sum check passed it. **Only running the counter as the genuinely last action catches that class.**

**✅ ENT THEORY DONE AND VERIFIED, 2026-08-14** — 30 of 30 chapters · 362 sections · **70,691 body words** · **all 1,720 ENT questions linked** · 0 dead ids · 0 wrong-chapter · 0 unlinked. **259 printed pages, mean 8.6/chapter**; only three exceed 10 pp — `ent-tonsils` 15, `ent-stridor` 12, `ent-csom` 11, **all three the earliest rewrites, done before the format settled, and the only trim candidates left.** Real `file://` boot: **0 console errors, 839 CSS rules, all 3 `max-aspect-ratio:5/4` blocks, 4 module cards, 2,097 questions, Clepsydra images loading.** Every chapter rendered and page-counted individually, not estimated. **⚠️ 25 §14.5 register rows are still OPEN — most are reconciliation work against already-merged chapters (Gradenigo → `ent-csom`, myringotomy → `ent-aom`, adenoids → `ent-tonsils`). Read the register before starting the reconciliation pass; do NOT assume ENT is closed just because every chapter exists.**

**⚠️⚠️ REAL EXAM DATES, supplied by the user 2026-08-13 — the "early September" estimate is dead:** **14 Sep** Peds OSCE · **17 Sep** Peds Paper 1+2 · **22 Sep** ENT · **27 Sep** Ophtho · **3 Oct** Neuropsych · **18–19 Oct** Peds Papers 1 and 2. `app\data\schedule.js` is rebuilt on them, 68 rows 13 Aug → 19 Oct, all 104 chapters named; the user must press **Reset to the original plan** to load it. **Projection 2026-08-14:** theory all four modules done ~**17 Aug**; then banks — Neuro ~7 Sep, ENT ~11 Sep, Peds ~12 Sep, **Ophtho ~25 Sep against a 27 Sep exam, the only tight one.** **⚠️ Remaining-question counts (ENT ~680, Ophtho ~1,800, Peds ~1,170, Neuro ~800) are EXTRAPOLATED from ENT's page ratios, ±30 % — nobody has counted the other three banks. Each chat must report its first real page count.** **NOTHING IS DROPPED — banks are deferred, not cut**; questions added later are purely additive (`S.answers` keyed by question id). **Stop rule: clean TOPIC boundaries, never mid-topic.**
**⚠️⚠️ THEORY FORMAT = `START-HERE.md` §14. The 2026-08-04 "new study source" brief and the first two §14s are ALL DEAD** — the user rejected 30–47 pp chapters as *"toooo much… a waste of time [that] contraindicates the whole purpose of the project."* **The cause was a RULE, not sloppiness: "every question answerable from the notes alone, including why each wrong option is wrong" forced ~450 distractor-rejections per chapter into prose, DUPLICATING each question's own `explanation`, which the app shows one tap away via `qs`. REVOKED.** Budget: **max( lecture words used , 25 × linked questions ), floor 600, cap 3,000** — but see rule (1) below: **the ceiling that actually governs is ~2,400 words / ~10 pp.** Two-sided floor: every important LECTURE fact **plus** every question's **key + discriminator** as a line or cell, never the distractor essay. Telegraphic, tables by default, no question ids in body text, one `Src:` line per section, `intro` ≤ 50 words. **⚠️ THE BUDGET IS A CEILING ON WORDS, NEVER A LICENCE TO DELETE A PROTECTED FACT — stop at the floor and itemise the cost.** **⚠️⚠️ §14.3a LAYOUT, ADDED 2026-08-14 — the user saw the printed PDF: *"it doesn't look apetising at all. Some parts just look like plain paragraphs."* `·`-CHAINED MEGA-PARAGRAPHS BANNED (~45 w/para, ~15 w/bullet); `### ` sub-headings exist and are REQUIRED past two entities per section; open every entity block and `**Term**` bullet with an EARLY COLON/em-dash — **`mdLead()` colours only the text before the first `:`/`—` within 44 chars, so a lead bold without one gets NO anchor.** `Src:` and whole-line `*italics*` are now auto-styled as caption and aside. **Layout only — removes no facts, budget unchanged.** ⚠️ `strong:first-child` was tried FIRST and painted whole paragraphs: the MEDIAN bold-opening line carries **71 chars** of bold, max 440.** §14.5 keeps the **deferral register**: a fact moved elsewhere is named with its receiving chapter and ticked when delivered — *an unrecorded promise is a deletion.*
**⚠️⚠️ ALL THEORY IS WRITTEN FROM THIS (ENT/hub) CHAT FOR ALL FOUR MODULES — user's decision 2026-08-14; the other three chats STAND DOWN on theory and resume only their BANKS.** Paste text + resume order: `progress\briefs\PASTE-theory-format-changed.md`. **Two measurements decided it: (1) THE USAGE POOL IS SHARED ACROSS ALL CHATS — 4 subagents died on session limits 13–14 Aug with only 2 of the hub's running, so more chats multiply failures, not throughput; (2) theory is SHORT.** **Measured over all 30 ENT chapters: ~9–25 min of agent time each, two concurrent, ~30 chapters in a day** — so the remaining **74 ≈ 2–3 days.** **The BANKS are the 16-day job.** **Hub PLANS, DESIGNS, VALIDATES, RENDERS AND COMMITS; OPUS SUBAGENTS WRITE. Cap 2 concurrent** — agents still die on session limits, but **a dead agent's file is usually complete: validate it before rewriting anything** (3 of 30 were recovered that way, 1 needed only its header corrected).
**⚠️ THEORY IS WRITTEN BACKWARDS FROM THE QUESTIONS AND THE OTHER THREE MODULES HAVE A THIN BASE** — ENT ~55 q/chapter, **Ophtho 4.3, Neuro 3.7, Peds 3.9**. Only importance-test 1 weakens; tests 2–4 carry it and all 150 lecture files are cached (ENT 34, Peds 64, Ophtho 27, Neuro 25). **⚠️ So for those three the LECTURE side of the floor does the work — expect TERM 1 to govern nearly always, and expect the `ent-otorrhea`/`ent-vocal`/`ent-swallow` shape (tiny question count, large inherited debt) to be the norm rather than the exception.** **⚠️ A RECONCILIATION PASS IS MANDATORY when each module's banks close** — re-check every chapter's new questions against its notes and patch; a diff, not a rewrite. **⚠️ §14.5's 25 OPEN ROWS are that work for ENT.** `examiner-patterns.md` is **ENT only**; do not manufacture one from 4 questions.
**Corpus 2,097 questions. Theory 30 of 104 merged — ALL of ENT, none of the other three.** Drafts at `content\ent\theory-drafts\`; **⚠️ EVERY `ent-*.draft.js` EXCEPT `ent-csom`/`ent-tonsils`/`ent-aom`/`ent-stridor` (whose current versions are the `*.v2`/`*.v3` files) IS NOW THE CURRENT FORMAT and safe to show a writing agent. The four bare originals of those names are the DEAD long format.** Best models to hand an agent: **`ent-nasalobs`** (three decks split by line range, three duplicate deferrals merged into one treatment), **`ent-sinuscomp`** (a chapter with no lecture, ~50 terms greped before tagging), **`ent-otorrhea`** (largest inheritance, smallest question count).
**⚠️⚠️ SEVEN RULES LEARNED WRITING ENT THEORY — all in `START-HERE.md` §14.1, and they are what makes the next 74 chapters cheap.** (1) **`pages ≈ body words ÷ 240`, a ±1-page estimator**; the true spread is 238–319 w/pp — **table-dense chapters cost MORE paper per word, prose-leaning ones less** — so divide by the densest so as never to under-predict. **The operative ceiling is ~2,400 body words / ~10 pp, NOT the formula's number**: eleven chapters shipped over budget (+17 % to +298 %) and every one was accepted on pages. (2) **⚠️ WORDS AND PAGES MOVE IN OPPOSITE DIRECTIONS WITH FORMAT, so neither can be gamed** — converting a table to prose *raises* the word count (the counter splits on whitespace) *and* costs pages. (3) **Predict the overrun before writing**: `(~90 × entities) + (framework slides costed ONE BY ONE) + (each grid at rows × columns)`. Lumping frameworks cost `ent-neck` 600 words of error; lumping grids cost `ent-phon` 487. (4) **Count only the part of a deck you use, BY LINE RANGE, claiming around what neighbours already spent** — and **deferring and counting are OPPOSITES**: whatever you hand on comes out of your TERM 1 (found when one deck was billed to two chapters). (5) **⚠️ THE DRAFT HEADER IS A CLAIM, NOT A MEASUREMENT — re-count from disk after the FINAL edit.** Caught seven agents; three self-caught. **Worst shape: a per-section breakdown that SUMMED EXACTLY to a stated total and was still 425 w stale** — a sum that reconciles proves only the numbers came from *one* state of the file, never the *last*. (6) **⚠️ A REGISTER ROW IS A CLAIM BY THE SENDING CHAPTER** — three were wrong in three different ways (over-specified the content, promised what the deck does not contain, **named the wrong deck entirely**) and two verified exact. Read the named deck, grep the stem, report a mis-stated row with evidence, never re-attribute quietly. (7) **⚠️ READ THE SLIDE BEFORE DECLARING A GAP — it has paid THIRTEEN times**, and **A SPELLING CAN HIDE A HIT TWICE OVER**: the decks write *ostio-* (`osteomeatal` → 0) and *"Uvelopalatopharyngoplasty"* (`uvulopalato` → 0). Grep the shortest stem, try the misspelling, and write both forms into the notes.
**⚠️⚠️ THE END-OF-ENT REVIEW'S "rests on outside knowledge" LIST IS ABOUT HALF WRONG AND MUST BE RE-GREPED ITEM BY ITEM BEFORE ANY BOOK TIME IS PLANNED ON IT.** Thirteen items checked: **eight found PRINTED** and used as sourced — the Cottle test, Samter's triad, the postcricoid risk in Plummer–Vinson, globus as a diagnosis of exclusion, bird's beak, the infantile larynx, Reinke's, and **five of the seven osteomeatal-complex items** the review calls absent. Ten confirmed genuinely absent at 0 hits and correctly tagged (agger nasi, lamina papyracea, nasal and laryngeal lymphatic drainage, the valveless venous route, rat tail/apple core, glomus, temporal-bone fracture types, aural foreign body/wax/syringing, Gerlach and Luschka). One half printed (the sneeze reflex is named, its arc is not). **The nose-book read that list justifies may be largely unnecessary.**
### The question work, frozen mid-flight and resumed after ~Aug 22 — **✅ Endpoint CLOSED. ✅ GG EAR + NOSE CLOSED. ✅ GG THROAT t1–t9 AND t11 WRITTEN. ⚠️ ONLY t10 LEFT IN GRADE GAIN — drafted, awaiting merge. THEN House.**
**⚠️ t10 IS STAGED BUT ONLY 11 OF 66 ARE DRAFTED — `content\ent\qb-pages\gg-throat-ch10.array.js` (all 66 verbatim, committed) + `.draft.js` (11 entries).** Subagent A died on the session limit. **An earlier note here called merging it "cheap"; that was wrong and is corrected — 55 entries still have to be written, so it belongs to the question resume, not to the theory sprint.** The staging is excellent and complete: 66 printed against 66 promised (**the 6th exact count in 23 topics**), **10 boxes** (Q527/529/532 on p.170; Q536/542/543/547/549/550/554 on p.171 — **those ten take NO marker**), **10 figures cropped** (`q-gg-163`…`-169`, one re-cut after catching its own caption), and **10 back-reference chains repaired**, three of them pointing at Q543. **Neither boundary defect fired here** — p.169 ends clean, p.172 opens t11.
**⚠️ WHEN QUESTIONS RESUME: House `d house ENT mcq.pdf` from PDF p.15 (prefix `enthd-`, 41 done to `enthd-ear-44`) — next is ch.3 "Otitis media & middle ear effusion", Q45 on, book pp.33–38 (PDF 17–20), render through p.39.** t10's answer page is **book p.171**, past the map's stated p.170, carrying **31 of its 66 keys and 7 boxes** — already found and already in A's draft.
**✅ TOPIC 11 CLOSED 2026-08-13 (`ledger.md` §14cc) — 21 printed, 21 written, 0 folded, 0 boxes; marker delta +21 exactly as predicted. `Q_ENT` 1,638.** **⚠️⚠️ THE ch.5 DOUBLE DEFECT FIRED ON ITS OTHER HALF, FIRST TIME IN FOUR TOPICS: Q584–Q587 are printed on p.174 IN THE LEFT COLUMN BESIDE THE ANSWERS BANNER.** t8 and t9 hid *keys* past the stated page; this hid *questions* on the answer page. **Both halves stay unpredictable in both directions — read every page in the range AND one past the last answer page.** **✅ The zero folds are EVIDENCED: 12 of 22 distinctive terms return 0 corpus-wide** (`talbot`, `voice handicap`, `grbas`, `inverse filtering`, `swallow screening`, `non-instrumental`…) — neither other bank teaches the voice-assessment protocol. **✅ AND BOTH STAGED FLAGS WERE SETTLED BY `L7`, NOT BY JUDGEMENT** (Q577, Q587 — FEES cannot see the oral phase and whites out during the swallow): **8th and 9th time "read the slide before declaring a gap" has paid.** One defect, a **new shape: Q580 offers "5 images / second" and "2 images each 0.4 seconds" — ARITHMETICALLY THE SAME NUMBER.** Answered on wording (`L8` prints the first clause verbatim); no key comparison could ever see it.
**⚠️⚠️ ENVIRONMENT TRAP THAT COST FIVE RUNS AND LOOKS LIKE A BROKEN BROWSER: HEADLESS CHROME AND EDGE BOTH DIE WITH `Multiple targets are not supported in headless mode` WHEN `--user-data-dir` POINTS INSIDE A PATH CONTAINING A DOUBLE DASH — and the session scratchpad is `…\Temp\claude\D--claude-os-Herophilus\…`.** Failed with `--dump-dom` and `--screenshot`, both browsers, old and new headless; identical command against `C:\Windows\Temp\cudx` worked first time. **Copy the instrumented `app\` copy AND the user-data-dir to a double-dash-free path.** The preview pane is no substitute — `javascript_tool` answers *"No site is open in this tab."*
**⚠️⚠️ SUPERSEDED FOR THEORY ONLY, 2026-08-14 — the other three chats STAND DOWN and the hub writes all theory (see the resume block above). EVERYTHING BELOW STILL GOVERNS THE QUESTION BANKS, which they resume, Ophthalmology first.** **⚠️ FOUR PARALLEL CHATS, ONE PER MODULE (user's decision, 2026-08-12), EACH RUNNING HUB-AND-SPOKE SUBAGENTS (same day, after interview): the chat is the SOLE WRITER of its data files and merges (sweep→fold→splice→validate→commit, one batch at a time); background Opus subagents draft one topic each into `content\<mod>\qb-pages\*.array.js`+`*.draft.js` and never touch data files or git. Protocol + prompt template: `START-HERE.md` §11. Cap 2 live subagents per chat (shared usage pool), scale to 3 after a clean day. ENT's slots: A = GG t10→t11, B = House from PDF p.15; main loop finishes t9 first. Theory later = one CHAPTER per subagent, per module as its banks close.** **This chat owns ENT and nothing else.** Briefs at `progress\briefs\` — `START-HERE.md` (shared method) + one per module. Each module chat owns a **disjoint file set** and writes its own `progress\resume-<mod>.md`; **`index.html`, `questions.js`, `modules.js`, `MEMORY.md` and `ledger.md` are OFF LIMITS to them** and all four modules' script tags and `cases.*.js` stubs are **already wired**, so none of them ever needs to touch a shared file. **⚠️ Stage only your own paths when committing — never `git add -A`** — and if git reports `index.lock`, another chat is mid-commit: wait and retry. **⚠️⚠️ WRITING BUDGET REVISED 2026-08-13 AT THE USER'S CHOICE, SUPERSEDING "FULL DEPTH EVERYWHERE" — full rules in `START-HERE.md` §12, and they change HOW MUCH IS WRITTEN, NEVER WHAT IS COVERED.** They asked to go faster *"without ignoring any important part"*, were shown the trade-offs, and picked **adaptive depth** + **write-once tables**: (1) keep ~520 words for clinical vignettes, defective/flagged keys, cross-bank divergences, gap-filled tagged answers and discrimination questions; **~250 words for straight slide-recall**, still carrying the key + verbatim quote, **why every distractor is wrong**, one clinical point and the citation. (2) A shared comparison table lives in ONE entry, siblings say *"full table in `<id>`"* — **⚠️ only ever pointing at an id that already exists.** (3) **`source` is a CITATION, not a second explanation** — file, page, box or not, lecture(s), where the tags sit, cross-refs; **⚠️ the marker still must never live there.** **The test is whether the chapter's theory notes could still be written from these entries.** **Subagent cap stays at 2 — the bottleneck is the shared usage pool, not slots.** **⚠️⚠️ AND THE USER CORRECTED THE FIRST DRAFT OF THE BRIEFS FOR CUTTING SCOPE — verbatim: *"I don't want in your brief to ignore any questions or any important thing in the theoretical notes. I did the separate chats to speed up not to have unfinished work."* Every bank AND the theory notes are in scope for every module; the compact-bank-first ordering is SEQUENCING ONLY** (endpoint 2,000–4,000 pages for ~700 questions = 4.4 each, against Grade Gain/House ~150 pages for ~1,000 = 0.17 each, measured 2026-08-12, a 25× difference). **If time runs short, say so loudly and leave the resume state clean — never trim a bank, thin an explanation or shorten the notes to make the remainder fit. Scaling down is the user's call.**
0. **⚠️⚠️ USER'S RULING, 2026-08-11 — A DEFECTIVE KEY IS NOTED, NEVER DISPUTED.** Verbatim: *"put the answer as Grade Gain says and put a note beside it that it's not mentioned directly in the lectures. **Do that with any questions that have errors like that, just leave a note beside.**"* `answer` is always the bank's printed key; the discrepancy is a short note **inside the `explanation`** saying what the lecture says or that none covers it. **This CLOSES the disputed-key roster as open work** — the nine in §11 become notes, not questions. **Never take a key dispute to the user again**; interview only for things they can supply (a missing file). **✅ NOTHING IS OWED AT THE INTERVIEW — the last item closed 2026-08-14: a NUMBERING GAP MEANS THE PROFESSOR WITHHELD THAT LECTURE (user's words), in every module. Never chase one; fill and tag.**
1. **GRADE GAIN IS OPEN — `ENT QB.pdf`, contents page promises 1,080** (Ear 244, Nose 258, Throat 578 over 29 topics; map in `ledger.md` §14). **⚠️ PDF page = book page + 9.** **⚠️⚠️ THE BOOK HAS THREE CHAPTERS, NOT TWENTY-NINE — 1 Ear, 2 Nose, 3 Throat.** What the contents page calls chapters are **topics inside a section**, so numbering runs continuously and **RESTARTS only at a section**. In the running header the **number is the section, the title is the topic** (`Chapter 1` on every Ear page was *correct*, not the defect earlier notes called it). **Ids must carry a section+topic token** — `entqb-ear9-236`, `entqb-nose1-1` — or Nose collides with Ear. **⚠️ QUESTION-PAGE RANGES RIGHT ALL TWENTY-ONE TIMES; COUNTS WRONG SEVENTEEN, ALWAYS UPWARD** — only 29/29, 20/20, 46/46, 7/7 and Throat 40/40 were exact; the rest ran over by 1–11. Trust the map for *where*, never for *how many*: render the range, then count what is printed. **Per-topic tallies are in `ledger.md` §14.** **✅ EAR 261 printed / 241 written / 20 folded** (244 promised). **✅ NOSE 265 / 247 / 18** (258 promised). **THROAT t1 40 (34+6); t2 47 (45+2); t3 61 (58+3); t4 99 (98+1); t5 53 (47+6); t6 32 (29+3); t7 45 (44+1); t8 66 (65+1); t9 57 (53+4); t11 21 (21+0).** **GG total: 1,047 printed, 982 written, 65 folded.** **⚠️ FOLDS ARE UNPREDICTABLE — run the full six-stage sweep on EVERY topic regardless of the last one.** **~30 % carry a printed explanation**; the rest authored — **⚠️ but ch.7 printed ONE box in THIRTY-TWO. Box presence is a property of the PAGE.** **⚠️⚠️ THE MAP'S "Page As" IS WHERE ANSWERS *BEGIN* AND NOTHING MORE — IT LIES AT BOTH ENDS, AND CH.5 HIT BOTH DEFECTS ON ONE SHEET.** A question tail can share that page (**six of fifteen** chapters: Ear ch.4 — which cost four questions — Ear ch.6, Nose ch.1, 2 and 5; not Ear ch.7–9, Nose ch.3–4 or ch.6), **and the answers can run PAST it** (ch.4 nearly lost **seven of twenty-three keys**; ch.5 did it again; ch.6 did neither). **Neither defect is predictable in either direction. Read every question page, and RENDER ONE PAGE PAST THE LAST ANSWER PAGE, EVERY TIME.** **⚠️ THE BANK ALSO REPRINTS ITSELF** — 6 within-bank folds take **no `alsoIn`**, just hold once. **⚠️ NEVER BACKTICK AN ID THAT DOES NOT EXIST YET** — caught **three times**, the last citing a *later chapter* rendered to find a boundary. **Reading ahead makes a forward reference feel safe. Re-run the dead-id check after every fold.** **✅ AND AT t7 IT WAS CAUGHT PRE-SPLICE FOR THE FIRST TIME** — eight references to the folded `…-377`, found by grepping the drafted blocks before splicing and rewritten to the fold target, so the post-splice check returned **0** instead of 8. **Seventh occurrence, first with no repair work afterwards. One command; always run it.** **⚠️ A FOLD CAN CONVERT AUTHORED TEXT INTO SOURCED TEXT** — **four times now**; the tag or marker must come off. **Spot it AT SWEEP TIME.** **⚠️ AND A FOLD CAN ENRICH THE HELD ENTRY** — ch.4's Q166 box was fuller than endpoint's own; fold the addition in. **⚠️ AUDIT THE MARKERS BOTH WAYS — a spurious marker is the same fault as a missing one** (ch.5 came out one OVER). It has caught a fault in most recent chapters; keep running it, and see item 35 on why the pre-splice version is blind to field placement. **⚠️ FOUR DEFECT SHAPES, ALL RECORDED AND NEVER "CORRECTED" — the printed key is right and the QUESTION is at fault.** (1) **Two correct answers**: Nose ch.1 Q11, Q19, ch.3 Q135 (saddle nose — Wegener's keyed, syphilis offered, and the bank's OWN Q116 keys syphilis; **ch.6 Q191 then offers "Both" and keys it**). (2) **An option that is a SUBSET of the key**: ch.4 Q160 keys the umbrella *complicated rhinosinusitis* over *orbital cellulitis*; ch.5 Q189 keys the specific. (3) **Two questions, two keys, because the MENUS differ**: ch.2 Q58 vs Q65, ch.3 Q128 vs Q130, ch.4 Q149 vs ch.3 Q98's box, ch.1 Q40 vs `entep-mfe4-31` — hold both, say to read the option list. (4) **A key contradicting a *STEM*, not another key**: ch.4 Q164 keys *throbbing* as NOT a criterion of sinus pain while Q155's vignette calls it throbbing. **No key comparison finds (4).** **⚠️ A SHARED TYPO IS STRONGER EVIDENCE OF A REPRINT THAN A SHARED ANSWER** — ch.2 Q66 reprints `entep-nose-65` down to "shenopalatine" in option b. **⚠️ AN EXPLANATION FIGURE ON THE ANSWER PAGE IS NEVER CROPPED** — `image` renders BETWEEN stem and options and would give the answer away. Describe it or transcribe it as markdown; only a figure printed **with the question** is cut. **⚠️ RE-CHECK A STAGED INTERVIEW ITEM AGAINST THE LECTURES BEFORE RAISING IT** — ch.3 Q145's "answered nowhere" was withdrawn once `L29) Rhinitis 1` was read, and **ch.8's Cottle test was staged as a gap and is printed on `L24`** — *"Deviations in region of nasal valve cause greatest obstruction : cottle test"*. **Fifth time; staging sees only the bank. READ THE SLIDE BEFORE DECLARING A GAP.** **✅ TOPIC 4 (98 written, `ledger.md` §14w):** stages A and B **both empty across 99 questions** — credible, the topic is adenoid and operative material endpoint barely covers — and its one fold was **WITHIN-BANK** (Q169 → `entqb-thr1-11`), which no cross-bank stage looks for. **⚠️ CROSS-BANK DIVERGENCE #2, DECIDED BY ONE WORD: `entqb-thr4-224` vs `entep-throat-77` share ALL FOUR OPTIONS and key different ones** — GG's *"enlarged **regular** tonsillar crypts"* carries a qualifier endpoint drops, so each key is sound on its own wording. **Both held, notes in BOTH.** **⚠️ A GENERIC "EXCEPT" STEM IS TO STAGE E WHAT A GENERIC OPTION MENU IS TO STAGE F.** Eleven defects recorded, incl. a self-contradictory *except*/*all of the above* stem, an option list where **no answer is correct in principle**, and a **printed box wrong about pharmacology**. Throat = **11 topics, 578 promised — larger than Ear and Nose combined.** **✅ t1–t9 AND t11 WRITTEN.** **521 printed / 494 written / 27 folded.** ⚠️ **ONLY t10 LEFT (66 promised) — subagent A drafting.** **✅ TOPIC 7 CLOSED 2026-08-12 (`ledger.md` §14z) — 44 written, marker delta +39 exactly as predicted.** Its one fold made `entep-throat-107` a question printed **three times across two banks** (endpoint p.1631, its own ENR reprint at p.2956, and GG p.142), held once. **⚠️⚠️ ITS DEFINING TRAP: TWO BACK-REFERENCE CHAINS SAT ADJACENT ON ONE PAGE AND MERGING THEM WOULD HAVE INVERTED AN ANSWER** — `Q362→Q361` is UNILATERAL palsy keying *observe + voice therapy*, `Q364→Q363` is BILATERAL keying *posterior cordectomy*, which widens the glottis at the cost of voice and would be frankly wrong for the unilateral patient. **`L11) Vocal cord paralysis.pdf` settled it independently** (cordotomy appears only under bilateral abductor management) — **and carried the whole cord-palsy chain verbatim, the seventh time "read the slide before declaring a gap" has paid.** **⚠️ Q367 keys "C = Bronchoscope" for an obstructing paediatric airway where Q363 keys "C = Perform a tracheostomy" for the same problem in an adult — a copied LETTER, not a considered answer. ⚠️ And Q373 contradicts the bank's own Q372** on mirror-vs-flexible laryngoscopy; the reconciling clause is trismus, which the bank never states. **Seven defects recorded per the ruling.** **⚠️⚠️ THE MARKER-IN-`source` FAULT HAS NOW FIRED THREE TIMES, AND THE THIRD WAS THE BIGGEST — FOURTEEN OLDER ENTRIES.** t2's 42 and t3's 47 markers all went into `source`; **the field-aware audit built for t4 passed the new batch at 91/98 before splicing and then found 14 pre-existing offenders** (`entqb-ear3-53`, `entqb-ear8-208/215/216/221/222/223/230/231/232`, `entqb-ear9-246/255/256/261`) — thirteen bolded mid-`source` right after the key citation. All relocated; `source` count now **0**, total occurrences unchanged, so nothing was lost or doubled. **⚠️ THE AUDIT MUST ASK *"does the entry's `explanation` END with the sentence"*, NOT *"does the entry contain it"*, AND MUST RUN OVER THE WHOLE FILE, NOT ONLY THE NEW BATCH** — the earlier fix pass missed these 14 because it looked for a missing marker rather than a misplaced one. **⚠️⚠️ FOURTH MARKER FAULT, t7, NEW SHAPE: SEVEN ENTRIES SIMPLY LOST THE SENTENCE AT WRITING TIME** — Q356/357/359/361/364/367/373, **scattered across four drafting files rather than in a block**, which is why nothing else saw them: every one parsed, cited and rendered fine. Predicted +39, measured +32, fixed to +39. **A block is visible when scrolling; singletons are not — the count is the only instrument.** Fix by anchoring on each entry's own `{ id:'…',` and the following `,\n  objective:` (`<scratchpad>\wrT7\fixmarkers.js`). **⚠️⚠️ AND NO LEVEL FIGURE IS REPRODUCIBLE ACROSS DEFINITIONS — TRUST THE DELTA, FIX THE DEFINITION IN A FILE. Two false alarms in five minutes at t7, both looking like catastrophic content failures:** (1) **baseline on `Q_ENT`, check on `QUESTIONS`** — the latter folds in the 82 cases, 45 of which carry the marker, so the delta read **+77** against a predicted +39; (2) **a probe omitting `.replace(/\*+$/,'')` under-counted by 239** — older entries italicise the marker, newer ones do not — reporting **743** where the same corpus reads **1,066 of 1,560**. **Canonical definition now in `<scratchpad>\wrT7\validate.js`, with a comment saying why the strip is not optional.** **⚠️ A SHORT GENERIC OPTION MENU MAKES STAGE F WORTHLESS — TWO CHAPTERS RUNNING.** t2 gave **three** 1.00 false positives (neck spaces, imaging modalities, referred-otalgia nerves); t3 gave **five more** at 0.80–0.82 (the *Zenker's / Plummer-Vinson / cricopharyngeal / oesophageal-ca* menu, five vignettes, three keys) **plus a false positive at STAGE B** (Q120 vs `entep-mfe5-43`, same options and key, unrelated stems). Real folds in both came from **stage E**. That is the reverse of Ear ch.1, where E scored 0.21 on folds F caught at 1.00. **Neither ranking dominates; run both, and read the list.** **⚠️ A MODEST F IS NOT EVIDENCE AGAINST A FOLD** (t2 Q76 folded at F 0.63 — every option repeats "abscess"), **AND NEITHER IS A MODEST E** (t3 Q147 scored 0.71 because its stem is a strict SUBSET of the endpoint one). **⚠️ TWO NEW DEFECT SHAPES.** (5th) **A FALSE DISTRACTOR IN A CORRECTLY KEYED QUESTION** — t2 Q64 offers chronic-RP drainage *"via transoral or external approach"* when `L4` says *"never through the mouth"*; wrong, **not the key**, so **no key comparison can ever see it**. (6th) **THE SAME ANSWER OFFERED TWICE** — t3 Q134 lists *"Rat tail appearance"* and *"Smooth, tapered narrowing"*, a name and its description. Also: t3 Q112 has **two true options** (`L10` says globus *"may be the only presenting complaint"*), Q139 keys a subsite the history does not predict because the right one is not offered, and Q138's printed box describes symptoms its own stem lacks. All recorded per the 2026-08-11 ruling. **⚠️⚠️ A VIGNETTE CAN BE PRINTED AS A HEADER OVER A NUMBERED BLOCK — NEW, AND IT BREAKS TRANSCRIPTION SILENTLY.** t3 has three (*"For the following questions 106, 107, 108 and 109"* + a vignette belonging to no question). **Eight questions would have had no vignette at all.** **⚠️ BUT NOT EVERY QUESTION UNDER A HEADER INHERITS IT** — t3's Q120 and Q121 sit inside a block and ask general questions; giving them the patient would invent one. **Back-references hit 17 of 61 = 28 %, the project's highest.** **✅ ALL SIX t3 FIGURE CROPS WERE RIGHT FIRST TIME — the first time ever, after 14 straight failures.** A **dark-region detector** did it: per column-half, find rows >55 % **non-white** (lum <232), merge runs separated by <45 rows, then bound columns the same way. **⚠️ MEASURE "NOT PAPER", NOT "DARK"** — a <110 threshold failed on two of six because the barium column is bright white and splits the run. X-rays and endoscopic photos are overwhelmingly non-white; text pages are 90 %+ white. **Still LOOK at every crop.** **⚠️ t4's single crop proved the detector reliable and the PADDING not** — it bounded the photo exactly (y 576–808, x 692–1095) and a 12 px symmetric pad still dragged in a strip of question text; **3 px was right**. Assets now **56 files, 2.3 MB**. **✅ `L1.1` AND `L1.2` ARE NOW CACHED — and they were IMAGE-ONLY, which is why they never were.** `pdftotext` returns only their title slides (306 / 330 chars); all 40 content slides were **rendered at 110 dpi and read as FOUR-UP CONTACT SHEETS — 10 reads instead of 40, and headings and bullets stay legible at 760 px per slide.** Use that trick for any image-only deck. Transcriptions are in `content\ent\lectures\`, each ending with an explicit **"NOT COVERED ANYWHERE IN THIS DECK"** list — which is what let Q2's suspected second answer be *settled by proving a negative* (neither deck states a vertebral level or labels the carotid bifurcation). **`L1.2` grounded eleven questions verbatim**, including the exact phrase *"Passawnt's bridge"* and the *"2nd 'pharyngeal' phase"* parenthesis Q9 turns on. **⚠️⚠️ FIFTH SILENT PARSE FAILURE — NEW MECHANISM: PowerShell's `''` ESCAPE EMITS A LITERAL APOSTROPHE**, which closes a **single-quoted** JS field. Three of them (*Henle's*, *entry's*, *this one's*) broke `questions.ent.js` while injecting fold notes. **⚠️ THE SIGNATURE IS DIAGNOSTIC: the validator reported ~36 dead backticked ids THAT OBVIOUSLY EXIST. A dead-id list full of real ids means THE DATA FILE DID NOT PARSE, not that the refs are wrong.** When injecting prose into a single-quoted field, write `\'` in the replacement. **⚠️⚠️ SIXTH AND SEVENTH: QUOTING A LECTURE PHRASE WITH *STRAIGHT* APOSTROPHES INSIDE A SINGLE-QUOTED FIELD** — `'including pneumonia'` ×3 and `'Kissing disease'`. **THE FIX IS NOT `\'` — IT IS THE SOURCE'S OWN CURLY QUOTES**, which `L3` actually prints (`“including pneumonia”`, `‘Kissing disease’`): more faithful *and* immune. **⚠️ `sed -i` with a `\\'` replacement SILENTLY DID NOTHING** — no error, file unchanged, confirmed with `cat -A`; use the Edit tool. **⚠️ Catch them before Chrome does:** `grep -n -o "[ (*\">]'[A-Za-z]"` over the block, minus field-delimiter hits, found both and nothing else. **A `<script src>` over `file://` still gives only "Script error. @line 0" — inline the data and it names the token** (*Unexpected identifier 'including'*, then *'Kissing'*), which is the fix itself. **✅ THE SECTION BOUNDARY IS SETTLED.** Book p.87 opens a **full-page "CHAPTER 3: THROAT" banner and the numbering RESTARTS AT 1** — confirming that the header's **number is the SECTION, title is the TOPIC**, continuous within a section. Nose ran unbroken Q1→Q265 under *"Chapter 2: <topic>"*. **⚠️ Throat ids need their own token — `entqb-thr1-1` — or they collide with Nose Q1 AND Ear Q1.** **⚠️ TWO ZERO-FOLD CHAPTERS IN A ROW, BOTH EXPLAINED BY ABSENT VOCABULARY.** ch.8: `cottle`, `osteocartilaginous`, `c-shaped`, `nasal valve`, `turbinate hypertroph`, `caucas`, `anterior dislocation` — all **0 hits**. ch.9: `cobblestone`, `turbinectomy`, `silver nitrate`, `gold standard`, `empty nose`, `submucous` — all **0**. Endpoint teaches DNS by *symptoms and complications*, never *classification, examination or operations*. **Say why a zero is a zero.** **⚠️ NEW PERMANENT TRAP: ANY TWO TRUE/FALSE QUESTIONS MATCH AT STAGES B, C **AND** F 1.00 BY CONSTRUCTION** (two-item option set, two-item key vocabulary). ch.8 Q256 vs `entqb-nose6-198` did exactly that; **only the STEM decides, and only stage E reads it** (0.17). **⚠️⚠️ SAME CLASS, WORSE: A BARE ORDINAL OPTION LIST CARRIES NO CONTENT AT ALL, SO B, C, D AND F MATCH ON NOTHING** — t7's Q345/Q355/Q374/Q375 all hit **1.00 at F** against each other *and* `entqb-thr4-159`, **which grades an ADENOID**; Q345 reached **stage B** too. *"Grade I/II/III/IV"*, *"X-ray/US/CT/MRI"* (third false positive in three chapters) and a generic *"which of the following"* stem (`entep-ear-126`, now **seven** captures) are all the same failure. **⚠️ AND AN IDENTICAL FOUR-OPTION MENU WITH A DIFFERENT KEY IS NOT A FOLD** — ch.8 Q258, Q250 and `entqb-nose7-210` share one menu and key three different answers. **⚠️⚠️ A LATER PRINTING CORRECTED WORK WRITTEN THE SAME DAY** — ch.9 Q259 keys *"SMR, **cocaine abuse**"* for the top two causes of septal perforation, while `entqb-nose8-249`, written hours earlier, had called trauma the commonest non-surgical cause. Both entries fixed in place. **First time the settled "a later printing resolves an earlier judgement" rule has fired inside one session. Read a whole section before writing any of it where context allows.** **✅⚠️ THERE IS NO `L28`, AND THE INTERVIEW ITEM IS NOW CLOSED — the user, 2026-08-14: *"The missing lectures were not given to me by the professor."*** So a **numbering gap means the lecture was WITHHELD, in every module** — never a capture failure, never a file to chase. **Do not re-raise one; fill the topic from general knowledge and tag it.** ENT holds 34 source PDFs / 34 cached, `… L27.2 → [GAP] → L29 …`; that slot is `ent-sinuscomp`, already written with no dedicated slide. **Peds is missing 21, 28, 29, 57 by the same answer.** *(`L1.1`/`L1.2` were gaps and are now cached.)*
1. **Then `d house ENT mcq.pdf`** (House, `enthd-`, resume at PDF p.15), then the **end-of-ENT review** below, then Pediatrics. **User chose "finish ENT completely" over breadth, 2026-08-08**, knowing the other three modules hold 0 questions and the exam is ~4 weeks out.
2. **PUBLISHING AGREED IN PRINCIPLE, PARKED UNTIL ENT IS DONE** (user, 2026-08-08). Progress survives updates because `S.answers` is keyed by **question id** in `wardround.v3.<profileId>` and adding questions is purely additive. **Three things BEFORE the first publish:** (a) **cache-bust the `<script src="data/*.js">` tags** — verified: **no version string**, so a returning browser serves the old copy and new questions silently never appear; (b) **fix the final domain first** — localStorage is per-origin, so a later move orphans all progress; (c) **finish folding first** — a fold retires an id, and an answered retired id orphans (harmless, but the count drops). **Publishing ENABLES Google sign-in**, impossible over `file://`. **⚠️ Two decisions open: the host** — Netlify was UNREACHABLE from the user's connection, so test first — **and whether the site is public at all**, given the content is from commercial banks.

**Question images** live at `app\assets\q\` — `q-<page>.jpg` for endpoint (cut by `Design\scripts\q-images.ps1`), **`q-gg-<book page>[a|b|c|d].jpg` for Grade Gain** (letter only where one page prints several), whose layout that script cannot read: crop by hand with `pdftoppm -x -y -W -H` and **LOOK** — **9 of the first 9 crops were wrong**, clipping a label or catching question or option text — **13 of 13 Grade Gain crops have now been wrong on the first attempt**, and Nose ch.7's four each caught question or option text before a clean second pass. **The trap is the LAYOUT, not the measurement.** **⚠️ A figure that EXPLAINS the answer is described or transcribed, never cropped** (Ear ch.3 Q61; Nose ch.3 Q104, Q112). `image` holds the **basename**, `qImgSrc()` builds the path; traps in `CLAUDE.md` §4, `ledger.md` §11a. **⚠️ `bundle.ps1` DOES NOT EXIST**; the single-file build is owed.
**⚠️ NEVER CARRY STRUCTURE ACROSS A SECTION BOUNDARY.** Endpoint sections and all 7 exams printed each question **twice** at a rate that ran **42–100 % with no trend**, Recently Edited **once already answered**, the fact-list run **irregularly**; Grade Gain prints **once**, ~10 per page, questions and answers on separate pages **that overlap**. Check the first ten pages before staging — **read EVERY page, never alternate ones, and ⚠️ SAMPLE FOR STRUCTURE, NEVER FOR RATE. The printed numbering lies, and ⚠️ NO CONTENTS PAGE COUNTS EITHER** — Grade Gain's held twice, then ran short **seven** times, once by **eleven** (§11b–d, §14). **Only reading every page counts.**
**⚠️ EVERY BANK MIS-KEYS ITSELF SOMEWHERE — GO LOOKING FOR IT.** Endpoint: 5 self-contradictions, **all on pages printing NO box**. Grade Gain: **three in nine chapters** — `entqb-ear1-26`, `entqb-ear3-79` and `entqb-ear5-141` (GSPN keyed to the lemon test; its own Q122 and Q140 say Schirmer's). **Mechanism decides: options replaced → new entry; options identical and only the key moved → record it in the existing entry, never a second; the bank contradicting its own explanation → correct the key and record what was printed** (`CLAUDE.md` §4). **⚠️ A PRINTED BOX CAN BE WRONG ABOUT SOMETHING OTHER THAN THE KEY** — ch.2 Q31 answers a different question, ch.3 Q75 calls Valsalva a drug. **⚠️ AND A KEY THAT LOOKS ARGUABLE MAY BE CONFIRMED BY THE OTHER BANK** — Nose ch.3 Q131's cacosmia key was flagged, then settled by `entep-nose-44`; **run the sweep before writing a dispute down.**

**Method: THREE passes.** **Grade Gain needs no tiling** — ~10 questions in two columns at `-r 130`; endpoint's tiling parameters are in `ledger.md` §7. **Order:** ENT → Pediatrics → Ophthalmology → Neuropsychiatry, questions then theory; **the ENT review below is owed before Pediatrics.**
**⚠️ THE THEORY BRIEF CHANGED, 2026-08-04 — it is a NEW STUDY SOURCE, sufficient to sit the exam from alone,
NOT a summary; length is never the goal, dropping something important is the one unacceptable failure.** Four
tests + drop-list in `CLAUDE.md` §4. User will add anatomy/physiology textbooks and offered a source interview —
**take it first**; **ask for a diagram rather than inventing one**. **⚠️ START FROM `content\ent\examiner-patterns.md`**
(166 facts; **repeats are the examiner's own weighting**) — it sets every chapter's `w` and `intro` and lists **6 named facts no transcribed question teaches**.

## What exists — **134 chapters**: ENT 30, Ophthalmology 26, Neuropsychiatry 27, **Pediatrics 51** (split 2026-08-15/16 from 21). **Theory written: 81 — all of ENT and all of Pediatrics.**
**⚠️ THE PARALLEL CHATS ARE PRODUCING — the other three modules are no longer empty.** Corpus **2,097** verified 2026-08-14: **ENT 1,720, Neuropsychiatry 158, Ophthalmology 138, Pediatrics 81.** By bank across the whole corpus: **1,180 Grade Gain, 697 Endpoint, 78 House** = 1,955 over 1,901, because **fifty-four** memberships are shared. **⚠️ The module keys are `ent`, `ophtho`, `pediatrics`, `neuropsych`.** **⚠️ OPTION COUNTS OF 8 AND 9 NOW EXIST (one each, from the parallel chats) — nothing in the app assumes four, but flag it if it grows.** **`entep-throat-103` is the project's first THREE-bank question** (endpoint + house + gradegain). **⚠️ A JUMP IN THE TOTAL IS NORMAL NOW AND IS NOT A BUG — check `byModule` before investigating.**

**Fully validated in a real `file://` boot, 2026-08-12** — 0 bad module/chapter refs, 0 duplicate ids,
every `answer` in range, nothing missing a `source`/`explanation`, **0 markers misplaced into `source`, 0 dead backticked ids over 5,578
references (1,425 distinct)**, all image files loading, none missing `imgAlt`
(a page's crop is legitimately shared by two entries), **0 console errors, 4 module cards, 768 CSS rules, all 3 `max-aspect-ratio:5/4` blocks, 9/9 Clepsydra poses**. **Canonical validator kept at `<scratchpad>\wrT7\validate.js`.** **⚠️ OPTION COUNTS OF 2, 3, 4 AND 5 COEXIST** — {2:5, 3:56, 4:1495, 5:187}; `entqb-nose6-198` is a TRUE/FALSE,
`entqb-nose7-226` prints three, and `entqb-thr8-438/440` print five; nothing in the app assumes four, verified. **⚠️ THE DEAD-ID CHECK CATCHES REAL DAMAGE, TWICE NOW** — a fold retires an id, and entries
written before it still cited it in backticks; the second time the id was one a fold meant was **never
written at all**. **⚠️ SO DOES THE MARKER COUNT: compute the expected delta FIRST** (ch.3: +44 new −1
withdrawn by a fold = +43, and it matched). **⚠️⚠️ FIFTH MARKER FAULT AT t8 AND THE FIRST IN THE OPPOSITE DIRECTION — SEVEN ENTRIES CARRIED A MARKER THEY SHOULD NOT HAVE**, one on every BOXED entry, reading 65 against a predicted 58. **A boxed entry takes NO marker** — confirmed by measuring the six existing boxed GG entries (`entqb-ear3-79`, `entqb-thr7-355/358/362/363/374`), none of which carries one, rather than from memory. **Computing the delta first works whichever way the error runs.** **⚠️ AND THE FIX ITSELF HIT A SHELL TRAP: a literal-string match on the marker, written through a bash heredoc, SILENTLY MATCHED NOTHING** — the em-dash is the sentence's only non-ASCII character. **Write the script with the Write tool and match by regex over the dash.** It has caught four earlier faults, the worst a **twelve-entry block
carrying no marker at all** — every entry parsed, cited and rendered fine, and nothing else would have seen it.
**⚠️ THE AUDITOR BREAKS BEFORE THE APP DOES — TWICE OVER, AND BOTH LOOK LIKE CATASTROPHIC CONTENT
FAILURES.** (1) **`MODULES` is `m.groups[].chapters[]`, each chapter an ARRAY (`c[0]` id)**, and **it
is declared `const`, so `window.MODULES` IS UNDEFINED** — read the bare binding. Either slip gives 0
chapter ids and marks **every** question a bad ref. (2) A dead-id regex of `` `ent[a-z-]+` `` **matches
CHAPTER ids too** (`ent-phon`, `ent-paedlar`) and manufactured 19 phantom dead references — require a bank
prefix and a trailing digit. **⚠️ ANY SYNTAX SLIP IN A DATA FILE FAILS SILENTLY AND LOOKS LIKE A CONTENT
BUG** — the app boots, throws nothing, and reports **82** questions (cases only) because `Q_ENT` never
parsed; **four times** — a missing comma, an unescaped apostrophe, unescaped apostrophes in fold notes, and
**`\\'` OVER-escaped** (which emits a literal backslash and then CLOSES the string — over-escaping fails
exactly like under-escaping, and the file's own consistency is no guard). **`file://` gives only "Script
error." at line 0** — inline the file into a scratchpad `<script>`. **⚠️ USE AN `addEventListener('error')`
PROBE, NOT `typeof Q_ENT`**: it reports **line, column and the offending identifier**, which is usually the
fix itself; subtract the preamble. **⚠️ RUN IT AFTER THE FOLDS AND AGAIN AFTER THE SPLICE**, so a failure
localises to two small edits rather than to 56 entries. **⚠️ A NAIVE QUOTE-BALANCE SCAN IS INVALID HERE** —
double-quoted fields legitimately hold apostrophes; it flagged 150 healthy lines. **The parse is the verdict.
A count that collapses to the cases alone means this, not content.**

**Lecture text cache**, 148 files in `content\<subject>\lectures\*.txt`; **check it before rendering** (10 extraction failures: §1) — **and LIST it before citing a filename, twice wrong from memory.** **⚠️ IT IS NOT COMPLETE AND THE SOURCE IS NOT EITHER** — ENT now holds **34 PDFs / 34 cached** — `L1.1` and `L1.2` were **IMAGE-ONLY** (verified 2026-08-11) and were read visually and cached 2026-08-11 and **`L28` EXISTS NOWHERE**; see resume item 1. `SCHEDULE` — 38 days to 2026-08-31.

## The app — boots clean from `file://`

Zero console errors, 9 Clepsydra poses, **846 CSS rules, all 3 `max-aspect-ratio:5/4` blocks intact**, **WCAG-AA clean**. **⚠️ FIXED 2026-08-14, all found by PRINTING a page and reading it: (1) `todayISO()` used `toISOString()` = UTC, so at UTC+3 the app showed YESTERDAY as today from 00:00–03:00 — it also mis-set every SRS due date and made `nextFreeDate()` return the last scheduled day again; five call sites now go through `isoLocal()`. (2) A markdown table whose header starts with a BLANK corner cell (`| | **A** | **B** |`) had its header eaten — the separator regex matched the single space — shifting every column label up one row across **150 tables**; `isMdRule()`/`mdCells()` fix it. (3) `mdBlock` never rendered blockquotes because `esc()` ran first. (4) Table headers now sit in a real `<thead>` (`display:table-header-group`) so a broken table repeats its labels; the zebra rule moved to `tbody tr:nth-child(odd)` to stay on the same rows.** The printed chapter is designed in the **module's colour** — full-page cover, section bars, table heads, tinted callouts, chapter name in the running head — via a `--mod` property and an injected `#printhead` style (Chrome supports neither `string-set` nor `var()` in an `@page` margin box). (Count **recursively**; match on `media.mediaText`, not a regex on `5/4` — Chrome normalises it to `5 / 4`; **subtract the harness's own `<style>`**.) Greek light/dark design from the user's own artwork, an ambient film, and **the Clepsydra** carrying the study-session timer; Import/Export replaced by automatic `wardround.bak.<profileId>` snapshots. **Every rule and trap is in `CLAUDE.md` §6. History: `ledger.md` §12, §11a. Backups: `archive\index.pre-*.html`.** Outstanding: **old module colours in Notion and the PDF schedule** (accepted), **no bundler**, **no cache-busting on the data `<script>` tags** — the last gates publishing (item 3).

**⚠️ THE ONE THING THAT MUST SURVIVE ANY SUMMARY: THE CLEPSYDRA IS THE USER'S OWN ARTWORK** — nine poses at `app\assets\clep\*.png`, source at `Clypsedra\`. **Do not redraw her, do not draw anything onto her.** Three rounds of wasted work in a day; the answer was always *ask for artwork*.

## Where transcription stands — **`ENT endpoint.pdf` DONE end to end; Grade Gain EAR and NOSE done, THROAT 8 of 11 topics done**

Endpoint boundaries and shapes: `ledger.md` §"Transcribed ranges" and §7; **Exam Night Review and the fact-list run closed 2026-08-08** (§11c–d). **Box presence is a property of the PAGE, not the section** — Throat ran 103 answered pages with none, ENR **zero of 104**, **4 of 4 mis-keys on a bare page**. **⚠️ For CASES the ANSWER decides fold-vs-variant, not the stem.**

## Four rules that change what you do first — **`ledger.md` §7 holds the rest (corrupt boxes, recycling, numbering, linked sets, layout and key defects). Read it before opening `ENT endpoint.pdf`.**

- **ASSUME NOTHING FROM PAGE TO PAGE**, and **NEITHER THE PRINTED NUMBERS NOR THE PAGE COUNT COUNTS THE QUESTIONS** — only reading every answered page does. **Check every tile for a highlight.**
- **RUN THE DUPLICATE SWEEP OVER THE WHOLE FILE BEFORE WRITING A BATCH.** The file recycles across sections; run *before* writing it has caught **557+ reprints with nothing wasted**. **Normalise first** (lowercase, punctuation, **sort the options**). **⚠️ SIX STAGES:** (A) stem + option set, (B) option set + key, (C) key + option count, (D) **≤1 DIFFERING TOKEN in the key** (symmetric difference), (E) **stem-token ranking over ALL of them**, (F) **OPTION-token ranking over all of them**. Rationale: `ledger.md` §7.
- **⚠️ STAGE F NOT OPTIONAL — STAGE E READS THE STEM ONLY**, so a question whose content sits in its **options**
  is invisible to it (ch.1: two folds at 1.00 that E scored 0.21/0.24). **⚠️ TWO BLIND SPOTS NO STAGE CAN FIX:** the
  filter drops words ≤3 chars, so short-code sets ("CHL/SNHL/Mixed") score 0 — or 1.00 for the wrong reason; and **an
  ABBREVIATED option set shares no tokens with a spelled-out one** — ch.4 Q98 folded into `entep-ear-199` at stage-F
  **0.13**. **READ THE STAGED LIST; the ranking is a shortlist.**
- **⚠️ A–D ALL KEY ON THE ANSWER, SO A REWORDED OR RE-OPTIONED REPRINT PASSES THEM ALL, and E and F list without
  deciding — proven on Exam Night Review, where the columns were wrong BOTH WAYS on 22 of 104.** **Read side by side
  EVERY staged question whose OPTION SET differs from its stem twin — stages C, D and clean TOGETHER**; **the new
  question need not be in the clean column** (14 of ENR's 37 were not). **A 1.00 stem match that did NOT reach A or B
  is a re-optioned reprint by definition**, and **a changed option set can move the key LEGITIMATELY** — not a
  self-contradiction, which needs IDENTICAL options. **A fold can carry something the held entry lacks.** Then
  keyword-search every survivor **twice, differently worded**.

**Where the source prints no explanation box, the explanation is written here** — rule and marker in
`CLAUDE.md` §4. **~64 % of ENT entries carry the marker** (867 of 1,342, counting *the phrase in
`explanation`* — the only reproducible definition) — most explanations are written, not transcribed,
**which is the real scope of the end-of-ENT review**;
Grade Gain improves on it at **~23 % printed** (177 of 773) — **but t4 printed only 7 boxes in 99.** **⚠️ READ EVERY OPTION AS A SENTENCE** — p.2856
printed one truncated mid-word and it survived staging and both sweep passes. **⚠️ AND EVERY STEM: a
reference to "the previous case" or to a picture must be repaired, because the deck is SHUFFLED** —
5 of 41 in Ear ch.3, **6 of 22 in ch.6**, 2 of 26 in ch.9, 1 of 46 in Nose ch.1, 1 of 22 in ch.5, 1 of 13 in ch.6,
**5 of 32 in ch.7** (including a **three-deep chain**, Q219→Q220→Q221), one pointing at a figure **the page never
printed**. **⚠️ A BACK-REFERENCE CAN CONTRADICT ITS OWN ANTECEDENT** — ch.6 Q203 says "this patient" of a man Q201
puts at 12 hours, then places him at 1 month. **⚠️ A STEM CAN HAVE NO QUESTION AT ALL** — ch.7 Q227 stops at the end
of its vignette; supply the sentence the options answer. **⚠️ OR ASK ITSELF TWICE** — ch.7 Q235 wants diagnosis *and*
next step over four diagnoses; strip the unanswerable clause. **⚠️ AND A STEM CAN HOLD TWO QUESTIONS WITH ONE OPTION LIST** — Ear
ch.9 Q245 (box supplied the missing half) and **Nose ch.3 Q145 (the LECTURE did)**. **⚠️ AND A KEY CAN
DROP ITS OWN DISCRIMINATOR** — Nose ch.1 Q29 offers Trigeminal **V1** and **V2** with the same efferent,
and the key prints neither qualifier; the letter still decides it. **⚠️ AND A STEM CAN PRINT ITS NUMBER
TWICE** — Nose ch.3 Q102, Q103, Q145, ch.6 Q197, ch.7 Q220 and Q231; strip it.

**⚠️ A GAP IN THE MATERIAL IS ANSWERED, NOT DECLARED — user's instruction, 2026-08-04, REVERSING the workspace rule for this project only.** Fill it from general medical knowledge and give the whole answer; keep `not taken from the course material` as a short tag on the claim, **never as a sentence that stops**. Shape to copy: `entep-throat-103`. **⚠️ Before leaving anything unanswered, INTERVIEW THE USER** — one batched list, never per sentence. Contract: `CLAUDE.md` §4. **⚠️ PREFER THE OTHER BANK TO OUTSIDE KNOWLEDGE where it prints the fact** — cacosmia came from `entep-mfe5-17`'s box twice.

**Three banks per module** (`endpoint`/`house`/`gradegain`); map, covers and the `alsoIn` contract in
`CLAUDE.md` §4. **Missing: House for Neuropsychiatry, Grade Gain for Pediatrics.** **⚠️ THE BANKS
OVERLAP — on a cross-bank match ADD THE BANK, never an entry.** **Twenty-six memberships over twenty-five
questions**; Grade Gain folds ran **4/29, 0/20, 3/41, 4/31, 1/35, 3/22, 0/18, 4/39, 1/26** across Ear
and **2/46, 4/38, 5/61, 4/23, 1/22, 0/13, 2/32, 0/23, 0/7** in Nose, and **6, 2, 3, 1, 6, 3, 1, 1, 4** across Throat t1–t9 — unpredictable, so **run the full sweep on every topic.** **⚠️ THE ONLY FOLD AT BOTH t4 AND t8 WAS WITHIN-BANK, WHICH NO CROSS-BANK STAGE LOOKS FOR** — ten within-bank reprints now, held once each.
**⚠️ STAGE A IS THE MOST FRAGILE STAGE, NOT THE STRICTEST** — ch.7's Q204 is word-for-word `entep-nose-69` except for a
leading *"The"*, and **A missed it while B caught it** (E and F both 1.00). The stages are different projections, not a
hierarchy. **⚠️ AND WHEN A STAGED QUESTION FOLDS, ITS NUMBER IS DEAD AT THAT MOMENT** — ch.7 shipped **ten** backticked
references to `entqb-nose7-232` because it was staged like every other question; **grep the drafted blocks for a folded
number BEFORE splicing.** Fourth occurrence, first at scale; the dead-id check caught it.
**⚠️ IN THREE CHAPTERS NOW THE FOLD REACHED NEITHER STAGE A NOR B** — ch.5's missed B by four words of a
spelled-out option and scored **1.00 on both E and F**. **The strict stages are a convenience; the rankings
are the instrument.** **⚠️ ZERO FOLDS IS CREDIBLE WHEN THE VOCABULARY IS ABSENT** — ch.6 returned nothing at A,
B or C, and `wing fracture`, `open book`, `angulated`, `lateral blow`, `boxing`, `medicolegal`, `walsham`,
`3d ct` and `osteotomy` all returned **0 hits corpus-wide**: endpoint prints nasal-fracture *timing* eight
times and the *classification* never. **Say why a zero is a zero.** **⚠️ THE TEST: A DROPPED (or ADDED) DISTRACTOR IS A REPRINT, A REPLACED ONE IS A NEW QUESTION** (§14c–d). **⚠️ A BANK ALSO REPRINTS ITSELF** — a within-bank match takes **no `alsoIn`**, it is just held once. **A folded id must lose its backticks everywhere it is still mentioned, and an id that does not exist YET must never be backticked either.** **⚠️ SAME OPTIONS + SAME KEY + DIFFERENT STEM IS NOT A FOLD** — ch.7 printed the commonest SNHL *in the elderly* and *in general* over one option list; both are held. **⚠️ NEW CATEGORY: CROSS-BANK DIVERGENCE** (ledger §11 item 7b) — two banks keying one fact differently is neither a mis-key nor a fold; hold both, reconcile in the text, take it to the interview. First: **commonest SNHL in children** — Grade Gain *hereditary*, endpoint *meningitis*. **⚠️ BUT CHECK THE MENU BEFORE CALLING IT ONE — A KEY IS THE BEST OPTION OFFERED, NOT THE BEST TREATMENT.** `entep-nose-97` keys *"within a week"* for the same 30-minute, no-oedema nasal fracture that three other printings reduce **immediately** — solely because its option list never offers "immediately". Not a divergence, not a mis-key. **Read what the list actually offers first.**

## Owed in the end-of-ENT review — detail in `ledger.md` §11. **⚠️ NO LONGER a gate: paediatrics theory started 2026-08-15 ahead of it, at the user's instruction. Work it when the ENT banks resume.**

1. **67 entries record no provenance either way** — 46 inside `entep-ear-1`…`68`, all 21 House. Almost certainly authored, **not verified**. **⚠️ Exams 1–3 box much of this range for free.**
2. **Re-run the duplicate check per module as it completes** — all **six** stages of it.
3. **⚠️⚠️ THIS LIST IS ABOUT HALF WRONG — RE-GREP IT ITEM BY ITEM BEFORE BOOKING ANY BOOK TIME.** It was written from the *questions*, never checked against the *slides*, and the theory pass has now checked thirteen of its items: **eight were printed all along** (see the resume block). The **osteomeatal complex** entry is the clearest failure — five of its seven components are printed, and the review missed them partly because **the decks spell it *ostio-*.** Genuinely absent and correctly tagged: **agger nasi, lamina papyracea, nasal and laryngeal lymphatic drainage, the valveless venous route to the cavernous sinus, glomus, temporal-bone fracture types, aural foreign body/wax/syringing, rat tail/apple core, Gerlach and Luschka.** Still unchecked and to be greped before use: the **nasopharynx set** (~27 entries — the biggest single claim), **fourteen Throat gaps** (Vincent's angina, leucoplakia — **the thyroglossal tongue-protrusion sign is now WRITTEN** in `neck-6`), **exostosis**, **acute necrotic otitis media**, the **mastoid compass / Citelli's**, the **Ear ch.8–9 set**, the **tongue's four-nerve table**, and the **Nose ch.3 set** (early/late phase, atopy triad, bone-vs-cartilage, Hebra nose, Mott cells, Type I/II atrophic, Samter's shunt, leprosy histology, iron/ketoacidosis, allergic mucin). **✅ `ent-sinuscomp` is now WRITTEN** — pre/post-septal, Chandler, Pott's, the diploic veins and the sinus→frontal / ear→temporal-and-cerebellar routes all delivered, ~50 terms greped first. **Interview shortlist** (§11, §14b, §14c, §14l, §14m).
4. **Nine flagged printed keys — all NOTES now, not open work** (the ruling in item 0). **Roster and reasoning: `ledger.md` §11.** Three rules survive. **⚠️ A FLAGGED KEY IS PROVISIONAL UNTIL THE BANK REPRINTS IT** — five settled that way, every one by a *later* printing. **⚠️ THE OTHER BANK CAN STRENGTHEN A FLAG WITHOUT SETTLING IT.** **⚠️ AND A KEY THE STEM CANNOT ASK PRECISELY IS A DIFFERENT THING FROM A WRONG ONE** (Nose ch.3 Q114). Answer as the bank does and say why.
5. **A full content and design review**, at the user's request, before the other three modules — **fold it into the pre-publish pass** (resume item 3), since both gate the same milestone.

## Environment — **full details in `ledger.md` §13.** What changes what you do first:

- **Poppler is installed** (off PATH) and handles the 332 MB file; **`pdftotext` returns 0 chars on every book and bank**, so scans render to PNG. **The read tool rejects PDFs over 100 MB.** **⚠️ `.ps1` FILES ARE BLOCKED BY EXECUTION POLICY** — pass PowerShell inline, never via a script file.
- **⚠️ NODE IS INSTALLED — v26.7.0, verified 2026-08-12** (this file said otherwise for weeks). Nothing needs it yet, but a JS validator or the owed single-file build no longer has to be hand-rolled in PowerShell. **No Python, ImageMagick, Ghostscript or `gh` CLI**; images via PowerShell `System.Drawing` (**⚠️ `Sort-Object -Property` silently no-ops on hashtables — use `[pscustomobject]`; `Measure-Object` returns Double, which the `Bitmap` ctor rejects — cast to `[int]`**), video via **ffmpeg** (off PATH). **Headless Chrome is the only way to screenshot or probe the app**; **Chrome + Canva for diagrams.**
- **The project is on GitHub, PRIVATE**: `mahmoudbanna23-ctrl/Herophilus`, branches `main` and `design/clepsydra-and-sessions` (the only copy of the Clepsydra artwork off this drive). `Semester 8\`/`files.zip` gitignored — **push method: ledger §13; sources §9.**

## Deadline and open items — **⚠️ SUPERSEDED: the real dates are in the resume block (Peds OSCE 14 Sep, papers 17 Sep, ENT 22, Ophtho 27, Neuro 3 Oct, Peds papers 18–19 Oct). The "early September" estimate is DEAD.**

- **Reading all 1,637 book pages is not realistic**, but **the book is the first place to look before filling a gap from general knowledge** — driven by the questions and slide gaps, via its contents page.
- **Measure, then believe.** *Check the sum* — a count that did not reconcile has been written down **six times** (`(Get-Content).Count`; `Measure-Object -Line` skips blank lines). *Check the auditor* — **three** contrast and **two** content auditors gave false failures (`CLAUDE.md` §7). *Reconcile the batch* — written + folded = printed. **⚠️ A COUNTER THAT DOES NOT MOVE IS A FINDING** — the authored-marker count stayed at 596 across 18 new entries because they carried the marker in `source`, not at the end of `explanation` where `CLAUDE.md` §4 puts it. **⚠️ NEVER WRITE A CROSS-REFERENCED ENTRY'S PAGE NUMBER FROM MEMORY — 7 of 7 were wrong** (§14g; the seventh, `entep-throat-111`, was drafted as p.1641 and is **p.1651**), and no automated check can see it: read that entry's own `source`. **⚠️ SAME FOR A LECTURE FILENAME — THREE TIMES NOW**: `L21) facial nerve.pdf` (twice, is `L15) Facial nerve disorders.pdf`), the rhinitis slides (are `L29`/`L30`), and `L28) complications of sinusitis` (**does not exist at all**). **List the directory before citing.** **And where no error is possible, LOOK.**
- **Cleanup awaiting approval** — a 173 MB Ophthalmology zip duplicating all 29 loose lectures, two
  Pediatrics PPT stubs (both inside protected `Semester 8\`), and `files.zip`. **Nothing without a yes.**


## Moved out of MEMORY.md 2026-08-19 — ophthalmology completion reporting

Cut when the file hit 222 lines against its 200-line cap, on the same rule that put the rest of
this archive here. Ophthalmology theory is finished; the full working detail is in
`content\ophtho\theory-plan.md` and `content\ophtho\book-map.md`. Every transferable RULE these
bullets carried was kept in MEMORY.md, compressed — only the module-specific evidence moved.

- ✅ **Ophtho finished at 36 chapters, not 26 — EIGHT splits**, each with its reasoning inline in
  `modules.js`: `op-orbit` · `op-cornea` · `op-cat` · `op-red` · `op-ret` **three ways** ·
  `op-refract` · `op-neuro` **three ways** · `op-intro`. **Only the first and last were split AFTER
  printing 14 pp — the expensive way; the rest were forecast before writing. Project total 144.**
  ⚠️ **Splits move questions** — 12, 1, 1, 16, 2 refiled — **always from the theory `qs` links.**
  ✅ **36 written and individually printed: 331 pp, mean 9.2** (ENT 8.6, peds 8.3); per-chapter
  table in `theory-plan.md`. **390 sections, 95,519 w**, 0 dead `qs` ids, 0 wrong-chapter, 0 dead
  section refs, **0 unanchored leads**, 0 over-cap paragraphs, **138/138 questions reachable.**

- **⚠️ `L9` IS READ BUT NEVER CACHED** (38 w of "CamScanner"×33) — its Hirschberg / Schirmer /
  Krause / Jones / tear-thickness highlights are the ONLY record of those five. **Cache as you read.**

- **Deck patterns (six) and three kinds of blank page — text slide · byte-identical filler · broken
  graphic** — are in `theory-plan.md`. ✅ **Filler sweep CLEAN NEGATIVE on all ten owed PDFs — DO NOT
  RE-RUN.** **~63 register rows are ophtho's**; ~20 closed, 13 notices, **6 undeliverable**, the
  rest open — **located work for the ophtho reconciliation pass.**

- ✅ **THEORY READING DEBT PAID — 11 book chapters cached** in `content\ophtho\book\` (ch.1 · 3 · 4 ·
  5 · 6 · 7 · 10 · 14 · 15 · 16 · 17, + ch.19 §(11) — **70,704 w**).

- ✅ **Both old interview items CLOSED 2026-08-18.** The `L31,32` QR code is **dead**; `L23`/`L37`
  are **NOT from this book** (ch.4 is Lacrimal, ch.9 is Lens). ⚠️ **I first argued that from "the
  book has no figure numbering at all" — FALSE**: ch.7 `Fig 7-1`, ch.3 `Fig (3-1)`, ch.19
  `Figure (19-1)` twice in 18 pp; ch.10/16/17 none. **Treat every "the book always/never…" line as
  a hypothesis until a chapter has been read end to end.**

### The ophtho bank count, in full (compressed in MEMORY.md 2026-08-19)

- ✅ **OPHTHO BANKS COUNTED FOR REAL 2026-08-18 — the ±30 % projection is dead.** `ophthalmology
  qb.pdf` (GG) = **1,087 total, ±0–5** (its contents page prints per-section counts and **matched
  the printed cumulative numbers at 4/4 independent checkpoints**); 138 done = exactly ch.1–3, so
  **949 remain**. `ophthalmology MCQ.pdf` (House) ≈ **1,025 ±30**; 56 done = exactly ch.1, so **~969
  remain**. **TOTAL ~1,918 REMAINING against 27 Sep.** ⚠️ **~304 are IMAGE questions** (House OSCE
  Q1–269 are each a clinical photo; GG End Exam 6 is 35) and **every crop is individually looked at
  — the dominant cost, larger than the transcription.** ⚠️ **56 House questions STAGED AND UNMERGED**
  — validated, 0 folds vs GG, all net new. House's **132 model-exam items are SHORT-ANSWER → the
  `type:'case'` route, not MCQ.** Both files' back halves are **past papers, recycled by design.**
  Detail: `ledger.md`.

### Bank blocks compressed out of MEMORY.md 2026-08-19 (rules kept there, arithmetic moved here)

- **Question banks, frozen mid-flight**: ✅ Endpoint closed · ✅ GG Ear+Nose closed · GG Throat
  t1–t9 + t11 written · **⚠️ only t10 left in GG** — staged complete (66/66 verbatim,
  10 boxes, 10 figures, 10 back-ref chains repaired) but **only 11 of 66 drafted**; 55 remain,
  belongs to the question resume, not theory. Then **House from PDF p.15** (`enthd-`, 41 done,
  next ch.3 Q45, book pp.33–38, render through p.39). GG totals: 1,047 printed / 982 written /
  65 folded. Detail per topic: archive + `ledger.md` §14.

- **GG ENT structure**: PDF = book + 9; three sections (Ear/Nose/Throat), **numbering restarts per
  section** — ids carry a section+topic token (`entqb-thr1-1`). ~30% carry boxes, but **box presence
  is a property of the PAGE**. **Back-references hit 28%** — repair every "previous case", the deck
  is shuffled (present in both ophtho banks too). Figures: **13 of 13 crops wrong first try — LOOK
  at every crop**; an answer-page figure is never cropped (it would reveal the key).


## Pruned out of MEMORY.md on 2026-08-22, verbatim

Removed when MEMORY.md hit 223 lines against its 200-line cap, after the nr-lmn split.
Each block survives in MEMORY.md as a shorter entry pointing here.

- **⚠️ `content\ophtho\theory-plan.md` + `book-map.md` still hold the ophtho reconciliation work**
  (~63 register rows, the `va-6`/`pup-10` provenance defects, five held divergences, `wht-8`).
  Do not re-derive any of it.

- **⚠️⚠️ CHECK A CHAPTER'S QUESTIONS AGAINST ITS SOURCE BEFORE BRIEFING IT.** The plan's Decks
  column is an assignment, **not a coverage guarantee**: **none of `op-va`'s 16 questions is
  answerable from `L3,4`**, its only listed deck — they are `L1,2`'s, lines 438–613 (866 w), now
  counted to `op-va` (**`op-intro`/`op-vissym` must not re-count them**). Same shape at `op-intro`:
  its 13 questions are **anatomy and physiology**, not the history-taking its title promises.
- **⚠️⚠️ AFTER A READ, A SOURCE'S WORD COUNT IS THE CACHE'S, NEVER `pdftotext`'s** — the `L7,8`
  reader advised against splitting `op-refract` on "1,540 words"; its own transcription held
  **4,439**, and the halves printed 10 and 8. **A text layer can be word-complete and still be
  wrong** (p71 filed PRK under lenticular surgery). **`pdffonts` per page detects text drawn as
  VECTOR OUTLINES** (`L31,32` p22: zero characters in every mode).
- **⚠️⚠️ COUNT A CHAPTER'S REGISTER ROWS AND VERIFY EVERY ROW'S *SECOND CLAUSE* BEFORE BRIEFING IT.**
  `op-ret` was named in **twelve rows from six donors and five were wrong** (~29 pp forecast against
  five questions — a question count predicts nothing). **TEN fault shapes**: over-specified · absent
  from the deck · wrong deck · wrong slide · circular · wrong receiver · **wrong line** · **stale, a
  third chapter delivered it** · **donor under-stating its own coverage** · **⚠️ LIVE POINTER TO AN
  EMPTY DESTINATION** — ch.17 → ch.16 for retinoblastoma resolves, and ch.16's section is **92 words,
  the only RB text in 274 pp**. **A resolved cross-reference is not evidence of coverage; measure
  the destination.** Evidence: §14.5, `theory-plan.md`.
- **⚠️⚠️ AUDIT "row filed" CLAIMS — FOUR WERE FALSE** (`uv-1`/`uv-8`/`uv-9`/`scl-1`; neither donor
  appeared in §14.5 at all). Rebuilt, marked reconstructed. **And a row's evidence has a TIMESTAMP:**
  `vis-11` shipped *"temporal arteritis and ESR return zero module-wide"* by quoting a row true when
  written; they return 9 and 8. **Re-run the grep, never quote another row's finding.**

- **⚠️⚠️ TEST-PRINT BEFORE ACCEPTING A SPLIT *OR* A COMPRESSION — the ÷240 estimator's two worst
  misses are the BOOK-SOURCED chapters**: `op-lid` forecast 16.1 pp printed **11**, `op-lac` 15.6 →
  **11**. Over-predicted by 5.1 · 5 · 4.6 · 3 · 2.3 · 2.1 · 2 · 2 · 1.5; under once (`op-glauc`).
  **A dying agent's last line was "Now the op-lac compression" — printing showed nothing to cut.**
- **⚠️⚠️ USER RULING 2026-08-18 — THE BOOK IS THE MODULE'S MAIN REFERENCE**, and the last decks
  (`L31,32`, `L33,34`, `L35,36`, `L37`) are **compilations of earlier chapters — ignore them, and
  any deck like them**. `ophthalmology.pdf` = **"OPHTHALMOLOGY WITH TUTORIALS", HEALIX TEAM**,
  274 A4 pp, image-only, **PDF = printed + 2**; contents, its own **exam-weight column** (a real
  budget basis where §14's formula collapsed) and per-chapter ranges in `book-map.md`, caches in
  `content\ophtho\book\`. **⚠️ `(Tutorial)` / `(T)` = "study in round & skip in final"** — record
  every marker; **NINE physical forms, and it is NEVER a licence to compress.**
- **⚠️ USER RULING 2026-08-18 — DO NOT ADD chapters for the three deckless book chapters** (Vitreous,
  Paediatric Ophthalmology, Drugs and the Eye; 10.5 % of the paper). Asked with evidence, answered
  *"Don't add them"* — **settled, do not re-propose.** Scope comes from the lecture series; the book
  is the authority on content, never on scope. ⚠️ **Reading ch.16 for `op-onc`'s retinoblastoma was
  NOT a breach** — the ruling bars adding chapters, not reading pages. Ophtho **finished at 36**.
- **⚠️ Treat every "the book always/never…" line as a HYPOTHESIS** until a chapter has been read
  end to end. Ophtho's figure numbering was called absent and is merely inconsistent.

- ✅ **OPHTHO BANKS COUNTED FOR REAL 2026-08-18 — the ±30 % projection is dead: ~1,918 REMAIN**
  (GG 949 of 1,087 ±0–5, House ~969 of ~1,025 ±30) against 27 Sep. ⚠️ **~304 are IMAGE questions and
  every crop is individually looked at — the dominant cost, larger than the transcription.**
  ⚠️ **56 House questions are STAGED AND UNMERGED** (validated, 0 folds, all net new); House's **132
  model-exam items are SHORT-ANSWER → the `type:'case'` route.** Arithmetic and method: `ledger.md`.

## Moved out of MEMORY.md 2026-08-23 (ps-anxiety), verbatim

### Reconstruction checks — the orphan-word fallacy

- **⚠️⚠️ "NO ORPHAN WORDS" IS NOT A VALID CHECK** — it proves every word landed SOMEWHERE, never in
  the right sentence. **Never accept a reconstruction on word conservation; render the page.**
  `pdftotext -raw` recovers slides whose text streams together. **Text-sufficient is a claim about
  PROSE, never about tables or columns.**

## Moved out of MEMORY.md 2026-08-23 (schizophrenia pair), verbatim

### End-of-ENT provenance list, and question-bank structure

- **⚠️ The end-of-ENT "outside knowledge" list is about half wrong** — re-grep item by item before
  booking book time (8 of 13 were printed all along); 67 entries record no provenance, roster in
  `ledger.md` §11. **Bank structure** (§14): **numbering restarts per section**, so ids carry a
  section+topic token; **box presence is a property of the PAGE**; **back-references hit 28 %** —
  repair every "previous case", the deck is shuffled; **13 of 13 figure crops were wrong first try
  — LOOK at every crop**; an answer-page figure is never cropped.

### Bank method, moved out of MEMORY.md 2026-08-23 (banks paused), verbatim

- **Compute the expected marker delta FIRST, then audit both ways** — five marker faults in five
  shapes (block-missing, in-`source` ×3, lost at writing, spurious on boxed entries; a boxed entry
  takes NO marker). The audit asks *"does `explanation` END with the sentence"*, whole-file.
  Validator: `<scratchpad>\wrT7\validate.js` (strip trailing `*`s — not optional).
- **Every bank mis-keys itself — go looking.** Options replaced → new entry; identical options with
  a moved key → record in place; bank contradicting its own box → correct and record. Cross-bank
  divergence: hold both — **but check the menu: a key is the best option OFFERED.**

### Bank sampling and provenance, moved out of MEMORY.md 2026-08-23, verbatim

- **⚠️ A BANK'S CONTENTS PAGE MAY PRINT PER-SECTION COUNTS — LOOK BEFORE SAMPLING** (GG ophtho's did,
  turning ±30 % into ±5). **Verify, never trust**; *"highest question number"* misleads wherever
  numbering restarts per section — three of the four files.
- **⚠️ The end-of-ENT "outside knowledge" list is about half wrong** — re-grep item by item (8 of 13
  were printed all along); 67 entries record no provenance, roster in `ledger.md` §11. **Bank
  structure and the crop rule: `progress\memory-archive.md`.**

### Register-row auditing, moved out of MEMORY.md 2026-08-23, verbatim

- **⚠️⚠️ VERIFY EVERY REGISTER ROW'S *SECOND CLAUSE* BEFORE BRIEFING** — `op-ret` was named in twelve
  rows from six donors and **five were wrong**. **TEN fault shapes: `progress\memory-archive.md`.**
  **⚠️ A RESOLVED CROSS-REFERENCE PROVES NOTHING — measure the destination** (ch.17 → ch.16 for
  retinoblastoma is **92 words**), and **⚠️ it may resolve to the WRONG section. Check the target.**
- **⚠️⚠️ AUDIT "row filed" CLAIMS — FOUR WERE FALSE** (`uv-1`/`uv-8`/`uv-9`/`scl-1`). **And a row's
  evidence has a TIMESTAMP** — `vis-11` quoted a zero-returns claim that now returns 9. **Re-grep.**
