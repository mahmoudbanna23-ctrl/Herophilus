# Ophtho endpoint Part 1, chat B journal (sections 15, 17, 18, 19, 21)

Chat B owns s15 Squint, s17 Malignancies of Eye and Adnexa, s18 Ocular Trauma, s19 Ocular Systemic Diseases and
s21 Diagnosis Keys. Chat A owns s11, s12, s14, s16, s20. s13 (Retina) and s22-28 (model exams) are deferred and
untouched. Chat B does not write `MEMORY.md`, `ledger.md` or `CLAUDE.md`; those updates are handed back at the end.
Parent journal: `progress/resume-ophtho-endpoint-A-chapters.md`.

Pipeline per section: stage by script (`Tools/omniroute/stage-pages.mjs`, gateway `gemini/gemini-3.1-flash-lite`),
normalise, page-by-page vision re-check by a separate-house subagent, commit staging + `_manifest.json`, duplicate
sweep, brief, `array.js`, `draft.js`, `val-oph-ep.js`, independent check, splice, boot-check, commit.

## Standing facts for this chat

- Codex is out of quota until 2026-09-20 18:21. Gateway `auto/coding` via opencode works but is shared with chat A:
  first-pass staging returned 503/504 on 24 of 32 pages; `retry-stage.mjs` (per-page, 15 s x attempt backoff) recovered all.
- House/Endpoint overlap is chapter-shaped and nothing folds mid-stream across banks: cross-bank matches against
  House `opmcq-*` are logged below and folded at the end. Within-bank matches against the live endpoint bank are folded now.
- The vision read is Gemini flash-lite; every section gets a Sonnet subagent re-read of the page images before staging is
  committed. That subagent found three staging defect classes in s15 (figure flags on tables and text boxes set true,
  leading letter prefixes on options, stems with a stray glyph). Expect them again.

## s15 Squint, pdf 1066-1097 (in progress, 2026-09-18)

- Staged and committed `7587dd4`: 32 pages (10 theory pages 1066-1075, 11 questions as unmarked/marked pairs 1076-1097),
  11/11 keys agree with the page images.
- Question count 11. Numbering n:1..11 follows printed_q exactly; no printing defect in the numbering.
- Within-bank folds, adjudicated: n:2 (p1078) and n:7 (p1088) are exact reprints (stem, options, key) of live
  `ophep-optics-refraction-9` (endpoint p.176, chapter op-refract). n:2 prints the same amblyopia box; n:7 is unboxed.
  Survivor is the live entry; `folded: [2, 7]` on the `SEC_P1` s15 row. The survivor's `source` should gain the reprint
  citation (pdf pages 1078 and 1088) at splice time.
- n:1 (p1076, "Fluorescein does not offer help to diagnose") is not a squint question; the book prints it at the head of the
  Squint pages. Drafted with chapter `op-intro-exam` (a live fluorescein question, `ophep-exam-eye-11`, sits there).
  Val allows a per-row chapter; sec-oph's single chapter field is documentation only.
- n:5 stem: both printings carry one stray glyph inside "management" (1 vs l undecidable); the cleaned word is transcribed.
  n:9 prints "heterotrpia" and n:10 a lowercase "which"; kept as printed. n:10 and n:11 print options A and D with identical
  text; keys are on A and B respectively; they share an option menu (a pair, not a fold).
- Cross-bank overlap with House, LOGGED, NOT FOLDED (fold at the end): n:3 ~ `opmcq-c15-5` (stem 1.000, options 0.889);
  n:4 ~ `opmcq-c15-15` (1.000/0.889); n:5 ~ `opmcq-c15-10` (1.000/1.000); n:6 ~ `opmcq-c15-7` (1.000/1.000);
  n:8 ~ `opmcq-c15-12` (1.000/1.000); n:9 ~ `opmcq-c15-14` (1.000/0.667); n:10 ~ `opmcq-c14-5` (0.788/0.857);
  n:11 ~ `opmcq-c14-7` (0.773/0.857); n:2/n:7 ~ `opmcq-c15-8` (1.000/1.000, the House entry is already the cross-bank
  survivor of a Ch.16 reprint).
- Candidates into chat A's unspliced sections: none available (no other staging files existed at sweep time).
- `array.js` was generated by script (`gen-array-s15.js` in the scratchpad), not by an LLM dispatch: a pure JSON-to-JS
  transform, byte-exact. `draft.js` goes to opencode `omniroute/auto/coding`. Brief: `content/ophtho/qb-pages/oph-ep-codex-s15-draft.brief.md`.
- `_manifest.json` was overwritten with s15's provenance (git history keeps s10's); a `provenance_note` records the 24
  retried pages and the vision re-check.

### s15 handoff, 2026-09-18 (step budget reached; s15 NOT spliced, s17/s18/s19/s21 NOT started)

State: array (11 rows) and draft (9 rows, ids 2 and 7 absent) committed as WORK IN PROGRESS. `val-oph-ep.js --part 1 15`
passes (ALL CHECKS PASSED, 1,547 words). Nothing spliced: `questions.ophtho.ep.js` untouched, still 258 live at last
measure (re-measure by loading the array). Draft came from opencode `omniroute/auto/coding` (pass 1 too thin at 71-108
words; rework `oph-ep-codex-s15-draft.delta1.brief.md` raised most rows to 139-251 words). Medical read of the rework found
defects still open, fix with one more delta brief (`opencode run -c -m omniroute/auto/coding`) or a direct edit, then re-run
the validator:

1. `ophep-squint-11` was NOT reworked: 71 words, no distractor explanation, no tag, and it carries the process line
   "(Note: ophep-squint-10 uses this same option list.)" with a bare id. Needs: plain sentence that options A and D print the
   same text and the printed key is the marked one; walk the stem (left hypertropia, weak depression in adduction, worse
   looking down, chin depression, head tilt to the RIGHT = away from the affected side) to left trochlear (fourth nerve) palsy
   of the superior oblique; explain abducent palsies (horizontal, abduction failure) as the distractors. Tag what the sources
   lack. Delete the id mention.
2. `ophep-squint-5` cites its source ("According to the course material", "The book explicitly states", "cited in the source
   material"): remove that language, state the facts plainly. Book (`ch15-squint.txt`, p.199-201 area): in paralytic strabismus
   from trauma or diabetes spontaneous resolution may occur within 6 months, surgery if it does not resolve; acute-onset
   paralytic strabismus is investigated for diabetes and MRI brain. "Microvascular ischemic mononeuropathy", "favorable
   prognosis" and the evolving-palsy surgery caveat are not in the book: tag them.
3. `ophep-squint-1`: tags are stuck onto sentence ends with no punctuation ("...incision not taken from the course
   material. For..."); reformat as "(not taken from the course material)". Untagged general claims: cobalt blue
   illumination, binocular indirect ophthalmoscopy / ultrasound. Check `L3,4) Clinical- Ocular examination.txt` first, tag only gaps.
4. `ophep-squint-10`: the face-turn sentence is muddled. A left abducent palsy: face turned to the left puts the eyes in right
   gaze, away from the lateral rectus field of action, so diplopia is reduced (tag). "right-sided signs" is vague: a right
   abducent palsy fails to abduct the RIGHT eye, esotropia of that eye, face turn to the right.
5. Untagged general claims remain in `ophep-squint-4` (microvascular ischemia, orbital floor fracture entrapment) and
   `ophep-squint-6`/`-8` (adult-onset strabismus, floor fracture): tag or trim.
6. `ophep-squint-9` reads correctly (key C kept; the book's intermittent-heterotropia sentence is quoted in the note).
   Sources say only "Heterotropia is another term for strabismus" (constant); fine.

Then, in order: independent check by a different house (opencode drafted, so `vibe` or a refuter run; NOT opencode
`auto/coding`), Claude reads the final text, `git status --short app/data/questions.ophtho.ep.js` must be clean,
`node tools/bank-harness/splice-oph-ep.js --part 1 15 --write` (expect 258 -> 267 if chat A has spliced nothing in between;
otherwise re-measure), add the reprint citations (pdf p.1078 and p.1088) to the `source` of live
`ophep-optics-refraction-9` in the same commit, `node tools\boot-check\boot-check.js` (expect 4049/81/4/153, 0 errors), commit
with `git commit -F <msgfile> -- <paths>`. Then s17 (1150-1181), s18 (1182-1244), s19 (1245-1331), s21 (1374-1390): stage with
`Tools/omniroute/stage-pages.mjs` (PATH must carry the Poppler `bin`, see the brief), retry 503/504 with `retry-stage.mjs`
(scratchpad, may be gone: it is a per-page retry, 15 s x attempt), Sonnet subagent image re-read before committing staging.

Scratchpad scripts used (session scratchpad, may not survive): `norm-staging.mjs`, `retry-stage.mjs`, `sweep-oph.js`
(usage `node sweep-oph.js <x.staging.json> [other-staging.json ...]`), `gen-array-s15.js`. If gone, rebuild from these notes:
`gen-array-s15.js` pairs each unmarked page with the next marked page of the same printed_q, key = marked page key_index,
expl only when box_printed.

Not written by this chat, handed back for the owner: `MEMORY.md`, `ledger.md`, `CLAUDE.md` updates (s15 state above; s17/18/19/21
still open; the sweep candidates list above for the final cross-bank pass).
